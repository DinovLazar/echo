# file-map.md

> Every meaningful file/folder in the repo, one line each, kept greppable so anyone can find where a thing lives. **Code updates this on every add, rename, or delete.**

## How Code maintains this
- Add a line the moment a file is created; update it on rename; remove it on delete.
- One line per file: `path — what it's for` (plain language).
- Group by folder. Keep paths exact so they can be searched.
- If this map and the real repo ever disagree, the map is wrong — fix it (and note the slip in the next completion report).

---

## Project docs (repo root)
- `ECHO-Project-Instructions.md` — the orchestrator rulebook; pasted at the start of every chat
- `ECHO-Plan.md` — the master spec for the finished game
- `ECHO-Phase-Plan.md` — the living index of every phase
- `ECHO-Decisions.md` — append-only decision log
- `CLAUDE.md` — Claude Code's project guide (read first, every session)
- `AGENTS.md` — vendor-neutral agent guide (mirror of CLAUDE.md)
- `README.md` — short public-facing description + build/run pointer
- `.gitignore` — Swift/Xcode ignore rules (build products, DerivedData, user state, .DS_Store)
- *(missing at kickoff: `ECHO-Notion-Checklist.md` — not present in the repo; flagged in the Phase 1.01 report)*

## Project state (`_project-state/`)
- `_project-state/current-state.md` — live repo snapshot (overwritten each phase)
- `_project-state/file-map.md` — this file
- `_project-state/00_stack-and-config.md` — append-only stack/config log with pinned versions
- `_project-state/completions/Part-X-Phase-YY-Completion.md` — completion-report template
- `_project-state/completions/Part-1-Phase-01-Completion.md` — Phase 1.01 (Scaffold) completion report
- `_project-state/completions/Part-1-Phase-02-Completion.md` — Phase 1.02 (First device install) completion report
- `_project-state/completions/Part-1-Phase-02-grid-layout-reference.svg` — deterministic layout reference for the hello-grid (NOT a Simulator screenshot)
- `_project-state/completions/Part-1-Phase-03-Completion.md` — Phase 1.03 (Grid + Move) completion report
- `_project-state/completions/Part-1-Phase-04-Completion.md` — Phase 1.04 (Fold — record & replay) completion report
- `_project-state/completions/Part-1-Phase-05-Completion.md` — Phase 1.05 (Collision + restart) completion report
- `_project-state/completions/Part-1-Phase-06-Completion.md` — Phase 1.06 (Room contents, level data & win) completion report
- `_project-state/completions/Part-1-Phase-07-Completion.md` — Phase 1.07 (Reset run & step back) completion report
- `_project-state/completions/Part-1-Phase-08-Completion.md` — Phase 1.08 (The first teaching rooms) completion report
- `_project-state/completions/Part-1-Hotfix-Shape-nonisolated-Completion.md` — Part 1 hotfix (`nonisolated` on the `Diamond` `Shape`) completion report
- `_project-state/completions/Part-2-Phase-02-Completion.md` — Phase 2.02 (The board's real look + motion) completion report
- `_project-state/completions/Part-2-Phase-03-Completion.md` — Phase 2.03 (The fold choreography & the death dissolve) completion report
- `_project-state/completions/Part-2-Phase-04-Completion.md` — Phase 2.04 (Generative move-audio — the signature feature) completion report
- `_project-state/completions/Part-2-Phase-05-Completion.md` — Phase 2.05 (Haptics) completion report
- `_project-state/completions/Part-2-Phase-06-Completion.md` — Phase 2.06 (Settings, persistence, echo-trail aid & guidance microcopy) completion report
- `_project-state/completions/Part-2-Phase-07-Completion.md` — Phase 2.07 (In-room HUD & solid walls) completion report
- `_project-state/completions/Part-3-Phase-01-Completion.md` — Phase 3.01 (The full campaign — rooms 11–20) completion report *(line was missing from this map at 3.01; added in 3.02 — see Part-3-Phase-02 report §3)*
- `_project-state/completions/Part-3-Phase-02-Completion.md` — Phase 3.02 (Echo Run — arcade survival mode) completion report
- `_project-state/completions/Part-3-Phase-03-Completion.md` — Phase 3.03 (Navigation shell — Title / Main Menu / Level Select / win & game-over overlays) completion report
- `_project-state/completions/Part-3-Phase-04-Completion.md` — Phase 3.04 (Final feel + performance — device-gated groundwork: the two transition fixes, the dev perf overlay, the feel audit) completion report
- `_project-state/completions/Part-4-Phases-01-02-Completion.md` — Phases 4.01 + 4.02 (the wait action — `Direction.stay`/`GameState.wait()`/Wait control/feedback — and rooms 21–25, the strategic/relocating-echo band) combined completion report
- `_project-state/completions/Part-4-Phases-03-04-Completion.md` — Phases 4.03 + 4.04 (the teleport mechanic — linked pad pairs, `resolveLanding`, format v2 `portals` — and rooms 26–30, the teleport band) combined completion report *(line was missing from this map at 4.04; added in 4.05–06 — see that report §3)*
- `_project-state/completions/Part-4-Phases-05-06-Completion.md` — Phases 4.05 + 4.06 (the mirror mechanic — two bodies on one reflected input, `MirrorGameState`, the format `mirror` block — and rooms 31–35, the mirror band ending on the room-35 campaign finale) combined completion report

## Xcode project (`ECHO.xcodeproj/`)
- `ECHO.xcodeproj/project.pbxproj` — the project definition (targets, build settings, synchronized groups: `ECHO`, `ECHOTests`, and `Levels` (room JSON bundled into the app target, D-025))
- `ECHO.xcodeproj/project.xcworkspace/contents.xcworkspacedata` — implicit workspace pointer
- `ECHO.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist` — workspace check defaults
- `ECHO.xcodeproj/xcshareddata/xcschemes/ECHO.xcscheme` — shared build/run/test scheme (committed)

## App source (`ECHO/`)
- `ECHO/App/ECHOApp.swift` — `@main` SwiftUI App entry point; hosts `ContentView`
- `ECHO/App/AppRoute.swift` — **new (Phase 3.03).** The single root route enum (D-059): `title` / `mainMenu` / `levelSelect` / `room(String)` / `echoRun` / `settings`. `nonisolated`/`Equatable`/`Sendable` so it drives `.animation(value:)`, compares in `.onChange`, and is usable anywhere. `room` carries the `Campaign.roomIDs` id — the one way a specific room is opened
- `ECHO/App/ContentView.swift` — **Phase 3.03: the root router.** Holds one `@State route: AppRoute` and switches `body` on it (D-059), retiring **all** interim scaffolding (the `showEchoRun` flag + `infinity` button, the debug `Next` room-cycle + `Clear`, the campaign-vs-EchoRun `Group` branch — all gone). Owns the session-long services — the persisted `SettingsStore` (now also the campaign solved set + the Echo Run high score), the `GuidanceController`, and the pre-warmed `AudioManager`/`HapticsManager` — derives the palette from `settings.invertEnabled`, and lifts `.environment(\.theme,…)`/`.tint` to the root so every screen inherits theme + invert. The campaign `GameState` for the open room (`roomState`) is built fresh in `openRoom(_:)` (`LevelLoader`, bare-board fallback) and held here so it survives re-renders; `RoomView` gets `.id(roomID)` so its win overlay never leaks across rooms. Screen flow `.title → .mainMenu → { .levelSelect → .room → win overlay } / .echoRun → game-over / .settings`, on a soft ~200 ms fade (`.animation(Motion.guidanceIn, value: route)`). At launch sets `audio`/`haptics` `isEnabled` from the prefs, `start()`/`prepare()`s both, and keeps them synced via `.onChange` (D-013/D-050/D-059). **Phase 4.05 (mirror):** `openRoom(_:)` branches on `level.mirror` — a mirror level builds a `mirrorRoomState: MirrorGameState?` and `.room` shows **`MirrorRoomView`** instead of `RoomView` while it is non-nil; a normal level clears it, so the single-body path (screen, board, engine) is exactly as before (D-074/D-075)
- `ECHO/Settings/SettingsStore.swift` — **Phase 2.06.** `@MainActor @Observable` `UserDefaults`-backed store: four `Bool` prefs `invertEnabled` (default false) / `soundEnabled` (true) / `hapticsEnabled` (true) / `echoTrailEnabled` (false), each loaded from its key in `init` and written through on `didSet`, so a relaunch restores the last state. The single source of truth `ContentView` owns and `SettingsView` edits; `UserDefaults` injectable for tests (D-050/D-051). **Phase 3.02** adds the persisted `Int` **`echoRunHighScore`** (key `echoRun.highScore`, default 0 — the Plan §6 second `UserDefaults` value) + `recordEchoRunScore(_:)` (keep-the-best, returns whether a new best), the single persistence point for the arcade high score (D-058). **Phase 3.03** adds the campaign **solved-rooms set** (key `campaign.solvedRooms`, default empty, persisted as a `[String]`) — `private(set) var solvedRooms` + `markSolved(_:)` (idempotent, never re-writes) + `isSolved(_:)` — riding this same wrapper, not a second store (D-061); the four `Bool` prefs + the high score unchanged
- `ECHO/Settings/SettingsView.swift` — **Phase 2.06.** The four-toggle Settings screen (Invert mode / Sound / Haptics / Echo trail) bound to the `SettingsStore` with hand-built `Binding`s, presented as a `.sheet` from the debug-bar gear; palette-styled (paper gradient, `ink` text/tint — monochrome toggle tint, never platform green). Minimal/functional, not a Part-3 polish pass (D-050)
- `ECHO/Guidance/GuidanceController.swift` — **Phase 2.06.** `@MainActor @Observable` model behind the guidance microcopy (handover §6): the `GuidanceHint` room→hint mapping (room-01→`swipe to move`, room-03→`fold to keep the door open`, room-06→`beware — it bites`), the `UserDefaults` **seen-once gate** (`consumeHint`/`hasSeen`), `enterRoom` (fires an unseen one-time hint; called by `RoomView` on room entry), `showEaten` (the recurring death caption), and the current `GuidanceMessage`. Foundation-only (no SwiftUI) so the mapping + gate are unit-testable headlessly. **Phase 3.03** removes the dead 5th string `GuidanceFeedback.blockedByGhost` and updates the header to "Four strings" (D-056 follow-through)
- `ECHO/Guidance/GuidanceOverlay.swift` — **Phase 2.06.** The SwiftUI overlay (mounted in `BoardView`'s board ZStack, hit-testing off) that renders the controller's `message` per handover §6: SF Pro Text 15 pt `.medium` +0.2 tracking, `text.guidance` colour, no box/shadow, lower-third placement (upper-third fallback when the player is low), opacity-only fade-in 200 ms `curve.easeOut` → linger (2200 ms hint / 1600 ms feedback) → fade-out 350 ms `curve.easeIn`, self-timed off the message id (D-052)
- `ECHO/Models/GameState.swift` — `@MainActor @Observable` board state: full room config as `let`s — dimensions, `start`, `exit: GridCoordinate?`, `echoBudget` (`.max` on bare board), `walls: Set`, `switches`, `doors`, `hazards` — plus player cell, turn counter, current run, echoes, and `hasWon`. Designated `init(...)` (all room fields defaulted) + `convenience init(level:)`. `move(_:)` (no-op on off-grid / wall / closed door / after win; commits → collision-first then win), `playerCollides(...)` (land-on OR cross-paths over **echoes + hazards**; swap live vs hazards, D-018/D-022), pure derivations `isWall`/`isCellHeld`/`isSwitchHeld`/`isDoorOpen`/`isClosedDoor` (D-019), `position(of: Echo)`/`position(of: Hazard)`, `fold()` (empty/budget/win guards, D-027), `restartRun()` (also the **reset-run** op, D-029) / `clearEchoes()` (both clear `hasWon`), and **`stepBack()`** — pure single-move undo: pops `currentRun`, decrements `turn`, replays `player` from `start`; no collision/win check, never touches `echoes`; no-op at turn 0, refused while won (D-028/D-030/D-031). **Phase 2.05** adds the additive, read-only **`lastMoveOutcome: MoveOutcome?`** (`{ stepped, died, won }`) signal, set **only** inside `move()` (`.died` before `restartRun()` so it persists through the rewind; `.won`/`.stepped` otherwise; a no-op leaves it unchanged) — no existing method/behaviour/test changed (D-049). **Phase 4.01** adds **`wait()`** — pass a turn in place: advance the shared turn, append `.stay` to `currentRun`, leave the player put, then run the **same** `playerCollides` check (a mover landing on the held tile is a death + `restartRun`); refused only while won; never checks the exit; `move(.stay)` hardened to a no-op so `wait()` is the single way to pass a turn (D-066/D-067). Pure, unit-tested (D-013..D-032, D-049, D-066). **Phase 4.03 (teleport):** stores `portals: [Portal]` + a derived bidirectional `padMap: [GridCoordinate: GridCoordinate]` (built by `buildPadMap`, debug-asserting exactly-two-distinct-cells per portal and no shared cell); `move(_:)` and `stepBack()` resolve the landing through the shared `resolveLanding` (guards run on the **resolved** landing — you can't teleport into a wall/closed door), and `isCellHeld`/`position(of:)`/the new `upcomingCells(of:)`/the echo branch of `playerCollides` pass `padMap` so echoes teleport; **hazards stay pad-blind** (D-036). With no portals (`padMap` empty) every behaviour is byte-for-byte as before (D-070/D-071)
- `ECHO/Models/Level.swift` — `nonisolated` Decodable room schema: `Level` (`id`/`name`/`width`/`height`/`start`/`exit`/`echoBudget` + `walls`/`switches`/`doors`/`hazards`; element arrays optional → empty), `Switch` (`id`,`cell`), `Door` (`id`,`cells[]`,`heldBy[]` AND-array), and `enum LevelLoader` (`load(_ id:in:) -> Level?`, flat + `Levels/`-subdir bundle lookup, nil on failure). The locked v1 format (D-024). **Phase 4.03 (teleport)** adds `nonisolated struct Portal { id; cells: [two] }` and an optional **v2** `portals: [Portal]` field on `Level` (decoded `decodeIfPresent ?? []`, in `CodingKeys`, defaulted `[]` in the direct init) — an absent key ⇒ no portals, so every v1 room JSON is byte-for-byte valid (D-072). **Phase 4.05 (mirror)** adds `nonisolated struct MirrorSpec { axis; startRight; exitRight }` and an optional **`mirror: MirrorSpec?`** field (decoded `decodeIfPresent`, defaulted `nil` in the direct init) — presence ⇒ a two-body mirror room (the top-level `start`/`exit` become the LEFT body's); absent ⇒ a normal single-body room, so rooms 01–30 decode byte-for-byte unchanged (D-076)
- `ECHO/Models/Hazard.swift` — `nonisolated` Decodable value type for a moving lethal cell: `id`, `start`, `path: [Direction]`, `loops` (defaults true; absent path → empty); `position(at turn:)` applies one path step/turn (empty/turn-0 → start; `loops:false` stands still when exhausted; `loops:true` indexes modulo path length). Lethal on contact, not solid, doesn't hold switches (D-021)
- `ECHO/Models/Echo.swift` — `nonisolated` value type for one folded run: stable `id: UUID` + recorded `moves: [Direction]`; `position(start:turn:)` is a pure function of start+moves+turn (exhausted echo stands still). A `moves` list may now contain mid-path `.stay` (Phase 4.01) — a zero-offset step that holds the tile for a turn, so a folded echo replays a dwell-then-relocate run exactly (no special-casing needed; doc-only update). **Phase 2.06** adds the additive pure `upcomingCells(start:turn:)` — the tiles at turns `turn+1…moves.count` (empty once exhausted) the echo-trail aid draws through; a mid-path `.stay` can repeat a cell (harmless for the trail, dedupe left as-is). `Identifiable`/`Equatable`/`Sendable` (D-014/D-051/D-067). **Phase 4.03 (teleport):** `position`/`upcomingCells` gain a `pads:` parameter (default `[:]`) and walk step-by-step through the new module-level `nonisolated func resolveLanding(from:step:pads:)` — **the single teleport rule** (a displacing step onto a pad lands on its partner; the partner is terminal/no re-fire; `.stay` never jumps; empty pads = the old offset sum). Shared by `Echo`, `GameState.move`, and `stepBack` so there is exactly one rule (D-070/D-071)
- `ECHO/Models/GridCoordinate.swift` — `nonisolated` value type for a grid cell (`row`, `column`; origin top-left); `Equatable`/`Hashable`/`Sendable`/`Codable` (decodes a level-JSON `{row,column}`)
- `ECHO/Models/Direction.swift` — `nonisolated` `String`-raw-value enum of the orthogonal moves (`Codable`/`Sendable`; raw values match the JSON hazard-path names); `offset` (row/col delta) + `init?(from:to:)` adjacency rule used by tap input. **Phase 4.01** adds a fifth case **`.stay`** (offset `(0,0)`) — the recorded "pass a turn in place" element issued **only** via `GameState.wait()` (`init?(from:to:)` never returns it; `move(_:)` refuses it); `allCases` is now five (D-067)
- `ECHO/Models/EchoRunState.swift` — **new (Phase 3.02).** The **separate, additive arcade engine** for Echo Run (D-057/D-058) — `GameState` and the campaign value types are untouched. `@MainActor @Observable final class EchoRunState`: an open `size`×`size` board (9×9, centre start), one continuous never-folded `recording: [Direction]`, the shared `turn`, the live `echoes: [RunEcho]`, `isOver`, the additive `lastOutcome`, and `score` (= `turn`, turns survived). `move(_:) -> RunOutcome` applies the edge-swipe **stall** (player holds, turn advances, every shadow steps, the trail does not grow), spawns a delayed shadow every `spawnPeriod` turns (default **8** — D-057; injectable for tests), and ends the run on **land-on OR cross-paths** (the swap branch is live here, unlike the campaign, because a stall breaks the parity lock). `pendingDeath(for:)` is the view's deferred-death predictor (mirrors the commit). `reset()` is Retry. Pure shadow positions reuse `Echo.position` (D-014). New pure value types `RunEcho` (`id`+`spawnTurn`), `RunOutcome`, `PendingDeath` are `nonisolated`/`Sendable`. Real, unit-tested
- `ECHO/Models/Campaign.swift` — **new (Phase 3.03).** `nonisolated enum Campaign` — the single source of truth for campaign room **order & identity** (lifted out of `ContentView`'s old `private static roomIDs`): `roomIDs` (**now the 35 `room-01…room-35` ids** in play order — grown from 20 by Phase 4.02's strategic-echo band, Phase 4.04's teleport band, and Phase 4.06's mirror band; **room-35 is the campaign finale**, D-065) + the pure helpers `contains` / `index(of:)` / `number(of:)` (1-based display) / `next(after:)` (nil at the final room, now room-35) / `firstUnsolved(solved:)` (the Level-Select "next to play" accent; nil when all solved). No new identity type (room id is the existing `String`); no unlock gating (D-060). Rides `Models/*.swift`, so it is in the headless test module — fully unit-tested (D-060/D-061/D-069/D-077)
- `ECHO/Models/MirrorGameState.swift` — **new (Phase 4.05 / D-074/D-075).** The **separate, additive two-body engine** for mirror rooms (the D-058 Echo Run precedent — `GameState` untouched). `@MainActor @Observable final class MirrorGameState`: the vertical split (`midColumn = width/2`, even width debug-asserted, each body's start/exit asserted into its own half), `leftBody`/`rightBody`, the shared `turn`, two turn-aligned recorded streams `currentLeftRun`/`currentRightRun`, `echoes: [MirrorEcho]` (a pair of plain reused `Echo`s per fold), `hasWon`, and the same `lastMoveOutcome` feedback surface as `GameState`. `plan(for:) -> MirrorStepPlan` is the **single movement rule** — reflected input per body (`Direction.horizontallyMirrored`, added here as an extension), the partial-movement allow (in-half / on-board / not wall / not closed door at the pre-step turn, D-038), both-blocked ⇒ no-op — executed by `move(_:)` **and** read by the mirror board's presentation so there is no view-side copy (the Phase 4.03 lesson). `wait()` holds both bodies (D-066 reused); `bodyCollides(previousBodyCell:newBodyCell:turn:)` is the per-body land-on + cross-paths predicate over every echo-body + hazard (the echo swap branch is **live** here — desync breaks the parity that made it dormant in `GameState`); either body's touch dissolves both (`restartRun`); win iff BOTH bodies sit on their own-half exits the same committed turn (collision before win, D-023; a wait never checks exits). `fold()` (budget/empty/won guards) banks the two streams; `stepBack()` pops both and replays both bodies via the pure `Echo.position` walk. Cross-half derivations: `isCellHeld` = either live body or any echo-body (left/right); AND-doors span halves (D-019/D-074). **No teleport** — every position walk uses empty pads (mirror × teleport is Phase 4.07). Fully unit-tested (`MirrorTests`)
- `ECHO/Views/ControlButtonStyle.swift` — **Phase 2.07.** The one reusable SwiftUI `ButtonStyle` for in-room action controls (D-054): a continuous-radius (`11`) chip — `theme.ink` @ 0.07 fill (→ 0.12 pressed) + `theme.tileHairline` @ 0.6 hairline border + `theme.ink` footnote label, ≥44×44 pt tap target, press-scale 0.96, 0.35-opacity disabled dimming — reading the palette via `@Environment(\.theme)` and the enabled state via `@Environment(\.isEnabled)`. Fills its container width (`maxWidth: .infinity`) with a single-line guard (`lineLimit(1)` + `minimumScaleFactor(0.85)`) so a row divides into equal, evenly-spaced shares. A `prominentFill`/`prominentLabel` variant renders a solid fill + paper-tone label (the solved *Next*, D-055). Body split into a private `Chip` view to read the environment; default-MainActor view-layer type (D-013)
- `ECHO/Views/BoardView.swift` — the real board (Phase 2.02 look + motion; **2.03 fold/death events**), driven by the `Theme` token layer in both Light and Invert: faint `tile.hairline` lattice with per-cell tap targets + element layers to handover §2 spec (walls = **flat solid `theme.wall` tile** [Phase 2.07 / D-053, was a top-light→bottom-dark gradient]; exit = gold active-goal ring + glow / ink default ring; door = solid closed bar vs two faint open stubs; switch = hollow ring vs filled-gold held circle + glow; enemy = red diamond [rotated rounded square] + inner core + outline + glow) + translucent recede-behind echoes + fully-opaque ink player (the largest piece and the only shadow-caster, §3). Step motion: `commitMove(_:)` reads the model's public predicates to pick slide-vs-snap-vs-dissolve — a survived step runs `withAnimation(Motion.step)` (player + echoes glide 120 ms `curve.standard` in lockstep; hazards retimed to 140 ms via `.transaction`) and bumps `stepTick` to fire the player squash-and-stretch + soft-snap and each hazard's anticipation lean-in (keyframe scales). **Phase 2.03:** a fold (detected via `.onChange(of: echoes.count)`) plays the §6c choreography and a **fatal step defers** the model mutation (`triggerDeath` → `BoardEffectsOverlay` plays the §6d dissolve → `finishDeath()` calls `restartRun()`); input is locked while an effect plays; the dissolving player + colliding echo(es) and the peeling new echo are hidden in the steady layers while the overlay owns them; reset/step-back/room-load still snap. **Phase 2.04** injects an `AudioManager` and fires it from the same paths (behind the same input lock): a survived step plays a chord of direction-pitched ticks — one per moving entity, derived read-only from the public positions (`playStep`) — plus `playSolve()` if it won; a fatal step plays `playDeath()`; a fold plays `playFold()`. **Phase 2.05** also injects a `HapticsManager` and fires it 1:1 on those same paths: `haptics.step()` on a survived non-winning step (else `haptics.win()` on the winning step — exactly one), `haptics.collision()` in the fatal branch (alongside `playDeath()`, since the fatal path never calls `state.move()`), `haptics.fold()` in `triggerFold()` (alongside `playFold()`, so refused folds stay silent — D-049). **Phase 2.06** takes two more injected params — `showEchoTrail: Bool` and `guidance: GuidanceController` — and adds two steady-board layers: the **echo-trail aid** (`echoTrail`/`trailDots` + the private `EchoTrailLayer`/`TrailDot`: faint `echo.base` @ 0.40 dots, 3 pt / 8 pt, along each echo's `upcomingCells`, beneath the player, hit-testing off; reveal-on with an 8 ms/dot stagger via `Motion.trailReveal`, fade-off 150 ms via `Motion.trailFadeOut`, mounted only while on or fading so off = nothing rendered), and the **`GuidanceOverlay`** (anchored to the board ZStack, above the pieces); `triggerDeath` fires `guidance.showEaten()` on every death (D-051/D-052). Engine untouched; the board reacts via Observation. Hit-testing off on every drawn piece; glows/shadow render outside the cell, never clipped. **Phase 3.03** adds one additive optional param `inputLock: Binding<Bool>? = nil` (default nil; previews/uncoupled call sites unaffected) + an `.onChange(of: fold != nil || death != nil)` that reports the board's fold/death input lock outward, so `RoomView`'s out-of-board controls honour the same `fold == nil && death == nil` lock instead of mutating `state` mid-effect (D-059). No rendering/effect internal touched — same additive-param pattern as `audio`/`haptics`/`guidance`. **Phase 4.01** adds an additive `waitSignal: Int = 0` param (default unchanging, so previews/uncoupled call sites are unaffected): each increment runs `commitWait()` — the **same** input-lock-guarded path a tap/swipe uses — which predicts a fatal wait via `playerCollides` and defers the death dissolve exactly like a fatal step (`finishDeath`→`restartRun`), else plays the calm `.stay` tick chord, a gentle in-place **breath** pulse (`waitPulseTick` keyframe + the `Breath` scale struct), and the step haptic, then commits `state.wait()` inside `withAnimation(.step)` so echoes/hazards glide while the player holds (D-066/D-068). **Phase 4.03 (teleport)** adds a `pads(cell:)` layer (drawn beneath the pieces, hit-testing off) rendering a **first-pass** monochrome `PadGlyph` (a `nonisolated struct: Shape` of four bracketed corners) at every `state.portals` cell — paired pads share the glyph; uses `theme.ink` at a quiet opacity, **never the accent**; `commitMove`'s `target` is now the `resolveLanding` result (the legality/fatal/death-contact prediction evaluates at the **resolved landing**, matching `GameState.move`, so a teleport death animates the dissolve rather than a mis-predicted step); the trail and the death-visual echo match are made pad-aware via `state.upcomingCells(of:)` / `state.padMap` (D-070; refine in a later Design pass)
- `ECHO/Views/EchoRunView.swift` — **new (Phase 3.02).** The arcade board screen for Echo Run, driven by `EchoRunState`. A small **dedicated** board (not `BoardView`, which is coupled to `GameState`/`Level`) that reuses the shared feel layer: `Theme`/`BoardMetrics` look, `Motion` lockstep slide + player squash, `BoardEffects` (`DeathEffect` + `BoardEffectsOverlay`) for the death freeze + particle fizz, and the campaign's player/echo styling. Renders a 9×9 lattice (swipe + tap-adjacent input), the trailing shadows, the ink player, and a live score; **defers** a fatal turn (predict via `pendingDeath` → play the dissolve → `finishDeath()` commits, saves the high score via `settings.recordEchoRunScore`, reveals the interim **game-over** panel with score/best/**Retry**/Exit). Wires per-move `AudioManager` ticks (player + stepping-shadow chord) + `HapticsManager` (`step()`/`collision()`). Self-contained (own paper background). **Phase 3.03** replaces the interim panel + `onExit` with the **real game-over overlay** (turns-survived score dominant, "best N"/"new best!", **Retry** → `reset()`, **Main menu** → `onMainMenu`) and routes the top-bar chevron + game-over both to the Main Menu via `onMainMenu` (D-059). The arcade board, the deferred-death dissolve, and `EchoRunState` are unchanged. **Phase 3.04** mounts `.debugPerformanceOverlay()` (D-063) — the stacked-shadow worst case for frame timing; no-op in release. Real
- `ECHO/Views/TitleView.swift` — **new (Phase 3.03).** The launch screen (Plan §4 screen 1): a full paper field with the large ink "Echo" wordmark in heavy negative space, the game motif (one solid ink square + one translucent-grey echo square offset behind, reusing the player/echo styling), and a small grey "tap to begin"; the whole screen taps → Main Menu. `Theme` tokens only; inherits invert (D-059)
- `ECHO/Views/MainMenuView.swift` — **new (Phase 3.03).** The Main Menu (Plan §4 screen 2): a small "Echo" wordmark over three `ControlButtonStyle` buttons — **Campaign** → Level Select, **Echo Run**, **Settings** — with generous vertical rhythm; nothing else. Pure layout of existing parts (D-059)
- `ECHO/Views/LevelSelectView.swift` — **new (Phase 3.03).** Level Select (Plan §4 screen 3): a 4×5 `LazyVGrid` over `Campaign.roomIDs`, each a bordered square tile showing its room number + the exit-ring motif — **open hollow ink ring = unsolved / closed filled ink disc = solved** (the shape-based solved mark, never colour alone). The single muted accent (goal-gold border + faint wash) tints **only** the one "next to play" tile (`Campaign.firstUnsolved(solved:)`; none if all solved). Back affordance ("Menu" chevron) top-left; **every** tile tappable → `onPick(id)` regardless of solved-state (free pick, no gating). Reads `SettingsStore.isSolved`/`solvedRooms` (D-060/D-061)
- `ECHO/Views/RoomView.swift` — **new (Phase 3.03).** The campaign room screen (Plan §4 screens 4–5): reuses `BoardView` as-is and composes the real in-room HUD around it (the 2.07 layout, D-054) — top strip (back chevron → Level Select · centred room number · live turn/echoes readout), the board, and a control row of **four** `ControlButtonStyle` buttons **Fold / Step back (disabled at turn 0) / Reset run / Wait** (the Wait button added Phase 4.01 — enabled whenever input is unlocked, **not** at turn 0; it bumps a `waitRequests` counter that `BoardView.commitWait` turns into `state.wait()` with its feedback, rather than mutating `state` directly — D-068). The `GameState` is passed in (owned by `ContentView`); a fresh identity per room (`.id`) keeps the win overlay from leaking. **Controls route through the input-lock-guarded path** — gated on `inputLocked`, mirrored from `BoardView`'s `fold == nil && death == nil` lock — so they never mutate `state` mid-effect (D-059). Calls `guidance.enterRoom` on appear; on `state.hasWon` marks the room solved (`settings.markSolved`) and shows the **win overlay** (the closed exit-ring mark; "Next room" in `solvedGreen` → `Campaign.next` / "Level select"; final room → "Campaign complete" + only "Level select"). **Phase 3.04 (D-064):** the win overlay now reveals after a short named **pre-delay** (`winRevealDelay` ≈ 0.45 s, via `.task(id: state.hasWon)`) so the solved board reads first, and "Next room" routes through `advance(to:)`, which raises an opaque **`paperField` cover** (`advancing`, `advanceCoverFade` ≈ `Motion.guidanceIn`) over the solved board **before** swapping rooms so the incoming board never flashes behind the outgoing scrim — both first-guess "tune on device" constants. Mounts `.debugPerformanceOverlay()` (D-063, no-op in release)
- `ECHO/Views/MirrorBoardView.swift` — **new (Phase 4.05 / D-074/D-075).** The two-body board for mirror rooms, driven by `MirrorGameState` — a **dedicated** view beside the untouched single-body `BoardView` (the Echo Run precedent), reusing the shared feel layer: `Theme`/`BoardMetrics`, `Motion` (lockstep slide, per-body squash — a blocked desynced body visibly holds still — and the shared wait breath), `BoardEffects` (**one overlay per half**, so each body's fold peel / death dissolve plays at its own tiles), and `AudioManager`/`HapticsManager` on the same commit paths. Draws the **centerline divide** (a quiet ink rule — `BoardMetrics.divideWidth`/`divideOpacity`, monochrome, never the accent), both halves' walls/switches/doors/hazards, **two gold active-goal exits** (the existing single-exit treatment, twice), both live bodies, each mirror echo as two grey bodies, and the echo-trail aid per body (private `MirrorTrailLayer`/`MirrorTrailDot`/`MirrorSquash`/`MirrorBreath` copies — deliberate D-075 duplication). Input: swipe = the input direction (left-body semantics); a tap adjacent to EITHER body steps that body toward it (the right body via `horizontallyMirrored`); the Wait control rides `waitSignal` (D-068). **One rule, no view-side copy:** `commitMove`/`commitWait` predict via the engine's own `plan(for:)` + `bodyCollides` (the Phase 4.03 lesson), defer a fatal input's mutation until the two dissolves play (`finishDeath` → `restartRun`), and report the fold/death input lock via `inputLock` (D-059). Death-echo matching is land-on only (the mirror swap-death shows no echo cameo — documented, Design-refinable). **First-pass visuals — Design-refinable**
- `ECHO/Views/MirrorRoomView.swift` — **new (Phase 4.05 / D-074/D-075).** The campaign room screen for mirror rooms — the same shell as `RoomView` (top strip with back chevron / room number / turn+echoes readout, the board, the four-button `ControlButtonStyle` row **Fold / Step back / Reset run / Wait**, the win overlay with the final-room "Campaign complete" branch and the D-064 win-reveal delay + advance cover) but driving `MirrorGameState` + `MirrorBoardView`. A deliberate small parallel screen (not a generic `RoomView`) so the verified single-body screen is byte-for-byte untouched; controls honour the same mirrored input lock (D-059/D-068); solved-state persists via the engine-agnostic room id (`settings.markSolved`), so a solved mirror room shows solved in Level Select like any other
- `ECHO/Views/BoardEffects.swift` — **Phase 2.03.** The transient SwiftUI **Canvas** effect overlay (no SpriteKit, zero new deps): `nonisolated` `FoldEffect`/`DeathEffect` descriptors (read-only captures of an event the engine already decided), and `BoardEffectsOverlay` = `TimelineView(.animation) { Canvas { … } }`, mounted by `BoardView` only while an effect is in flight. Draws the **fold** (grid ripple in `tile.hairline`, then the new echo peeling from the run-end back to `start`, ink→`echoBase` crossfade, 32→28 pt) and the **death** (the player + colliding echo(es) glide onto the contact tile → calm freeze → bounded deterministic particle **fizz**, ~14 ink + ~10 grey per echo → faint `dangerRed` @ 0.08 vignette + one `dangerGlow` enemy pulse). Every colour from the injected `Theme` (both palettes); timings/curves from `Motion.Span`/`Ease`
- `ECHO/Views/PerformanceOverlay.swift` — **new (Phase 3.04 / D-063).** The developer-only frame-rate readout, gated **`#if DEBUG`** (compile-time) so it is **absent from any release build** and can never appear in the shipping UI. `View.debugPerformanceOverlay(_:)` (a no-op in release; mounted on `RoomView` + `EchoRunView`) overlays a top-trailing, **non-interactive** (`allowsHitTesting(false)`) neutral translucent pill — deliberately **not** `Theme` chrome (preserves the D-055 discipline) — showing FPS, instantaneous/avg/worst frame-time (ms), and a dropped-frame count. `FrameRateMonitor` (`@MainActor @Observable`) samples a `CADisplayLink` (iOS-only, behind `#if canImport(UIKit)`; off-iOS a no-op skeleton like `HapticsManager`), records each frame into the pure `FrameWindow`, and republishes a `FrameStats` snapshot a few times a second (cheap). No external packages (D-013/D-040/D-063)
- `ECHO/Diagnostics/FrameStats.swift` — **new (Phase 3.04 / D-063).** The pure, Foundation-only math behind the overlay: `FrameWindow` (a fixed-capacity rolling window of frame intervals with `record`/average/worst/fps/`droppedCount`/`summary`) and the `FrameStats` snapshot. `nonisolated`/`Sendable` like the engine value types, so it rides the headless test module and is unit-tested (`FrameStatsTests`); **not** `#if DEBUG`-gated (harmless pure code, never instantiated in release). Lives in `ECHO/Diagnostics/` — a dev instrument, not a game model — but a pure value type in the same spirit
- `ECHO/Diagnostics/` — **new (Phase 3.04)** developer-instrument value types (above); rides the `ECHO` synchronized root group, no `.pbxproj` edit
- `ECHO/Theme/Theme.swift` — **the central colour-token source** (Phase 2.02): `Color(hex:)` helper; `ThemeMode` (light/invert); `Theme` struct holding both authoritative palettes for every handover §1a token (defaults to Light); `BoardMetrics` (all §1b geometry/stroke/opacity/shadow/glow values, in pt at the reference cell C = 44); and the `\.theme` `EnvironmentValues` key — the single switch point **2.06 now binds** via `SettingsStore`. **Phase 2.06** adds the additive echo-trail tokens to `BoardMetrics` (`trailDotSize` 3 / `trailDotSpacing` 8 / `trailDotOpacity` 0.40). **Phase 2.07** replaces the `wallTop`/`wallBottom` gradient pair with one flat `wall` token per palette (Light `0x312D29` / Invert `0x2F2B25` — D-053) and adds the UI-chrome `solvedGreen` token (Light `0x5C8A52` / Invert `0x77B86A` — the solved-Next fill, chrome only, never on the board — D-055). All `nonisolated`/`Sendable` (D-013/D-040/D-051). **Phase 4.03 (teleport)** adds the first-pass pad-glyph tokens to `BoardMetrics` (`padSize` 30 / `padCornerArm` 0.32 / `strokePad` 2.5 / `padGlyph` opacity 0.50) — ink, monochrome, not the accent (D-070). **Phase 4.05 (mirror)** adds the first-pass divide tokens (`divideWidth` 2 / `divideOpacity` 0.30) — the mirror centerline rule; ink, monochrome, not the accent, Design-refinable (D-074)
- `ECHO/Theme/Motion.swift` — **the named motion curves** (Phase 2.02; **2.03 additions**): `Curve` = the five handover §6a easing curves (`standard`/`easeOut`/`easeIn`/`softSnap` as duration-parameterised `timingCurve`s with the exact control points; `decayShake` as the `interpolatingSpring(stiffness:320,damping:14)`); `Motion` = the §1c motion tokens built from them (`step` 120 ms, `stepSnap` 40 ms, `enemyStep` 140 ms; fold/death/deny/guidance/trail `Animation` tokens). **Phase 2.03 added** `Motion.Span` — the §1c phase durations as raw seconds (`foldHitPause`/`foldRipple`/`foldPeel`/`deathFreeze`/`deathFizz`/`deathVignette`/`step`) for the Canvas layer (which advances by elapsed time, not a baked `Animation`) — and `Ease` — the §6a curves as scalar cubic-Bézier easings (`standard`/`easeOut`/`easeIn` + a `bezier` solver) so canvas-driven effects share the exact curve shapes. **Phase 2.06** adds `Motion.trailFadeOut` (150 ms easeIn) and `Motion.Span` raw-seconds for the trail fade + guidance fades/lingers (`trailFade`/`guidanceIn`/`guidanceOut`/`guidanceLingerHint` 2.2 / `guidanceLingerFeedback` 1.6). All `nonisolated`
- `ECHO/Audio/AudioManager.swift` — **Phase 2.04.** The generative-audio manager: a `@MainActor final class` owning one `AVAudioEngine` + a pool of 16 `AVAudioPlayerNode`s into the main mixer, playing short `AVAudioPCMBuffer`s **synthesized once at startup** (no bundled audio — D-045). `start()` configures the `AVAudioSession` (`.ambient` + `.mixWithOthers`, iOS-only behind `#if os(iOS)` — D-047), pre-warms (`prepare()`) and starts the engine, and keeps the pool playing so a fire is one sample-aligned `scheduleBuffer(at:)` at a shared future host time. Sound bank: four **direction-pitched** marimba-like step ticks on a fixed pentatonic set (up higher / down lower / left-right middle — D-046), a warm **fold** settle, a calm muted **death** puff (low tone + filtered noise), and a gentle ascending **solve** figure. `playStep(directions:)` fires a turn's ticks as one chord; `playFold`/`playDeath`/`playSolve` align to `Motion.Span` beats. `var isEnabled` (default `true`) gates all playback — the single Settings hook for 2.06 (no toggle UI/persistence here). **Phase 4.01** adds a fifth step tick for **`.stay`** (a low, quiet, calm C3 under the pentatonic set — `buildWaitTick()`, keyed into `stepBuffers[.stay]`) so a wait voices through the same `playStep` chord path and an echo replaying a recorded wait sounds it (D-067)
- `ECHO/Audio/` — now populated (above); generative percussion, Part 2 (Phase 2.04)
- `ECHO/Haptics/HapticsManager.swift` — **Phase 2.05.** `@MainActor final class` owning the three system `UIFeedbackGenerator`s (selection/impact/notification) behind `#if canImport(UIKit)`; `step()`/`fold()`/`collision()`/`win()`; `isEnabled` gate (the 2.06 haptics-toggle hook). Real
- `ECHO/Settings/` — **Phase 2.06** persisted preferences + Settings screen (above); rides the `ECHO` synchronized root group, no `.pbxproj` edit
- `ECHO/Guidance/` — **Phase 2.06** guidance-microcopy controller + overlay (above); rides the `ECHO` synchronized root group, no `.pbxproj` edit
- `ECHO/Resources/Assets.xcassets/Contents.json` — asset catalog root
- `ECHO/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json` — app icon set (no artwork yet)
- `ECHO/Resources/Assets.xcassets/AccentColor.colorset/Contents.json` — accent color (system default)

## Levels (`Levels/`)
> The ten teaching rooms (Phase 1.08, D-033) **plus the ten Part-3 campaign rooms (Phase 3.01), the five Part-4 strategic-echo rooms (Phase 4.02, D-069), the five Part-4 teleport rooms (Phase 4.04, D-073), and the five Part-4 mirror rooms (Phase 4.06, D-077)** — the full `room-01 → room-35` campaign, ending on the room-35 finale (D-065). Rooms 01–25 are the locked v1 format (D-024); rooms 26–30 add the additive **v2 `portals`** field (D-072); rooms 31–35 add the additive **`mirror`** block (D-076) — every earlier room JSON is byte-for-byte unchanged. The three throwaway proof rooms (`p1-06-a/b/c.json`) were deleted — no test referenced them and they were replaced in the play sequence. All bundled into the app target via the `Levels` synchronized root group (D-025).
- `Levels/room-01.json` — "Straight Line" (move; budget 0)
- `Levels/room-02.json` — "The Turn" (walls; budget 0)
- `Levels/room-03.json` — "First Fold" (echo holds a switch → door; budget 1)
- `Levels/room-04.json` — "Mind the Past" (echo is a lethal obstacle, route around it; budget 1)
- `Levels/room-05.json` — "Two Selves" (two echoes hold two doors in series; budget 2)
- `Levels/room-06.json` — "The Patrol" (time a moving hazard; budget 0)
- `Levels/room-07.json` — "Hold, Then Time" (hold a door open while timing a patrol; budget 1)
- `Levels/room-08.json` — "Two Jobs" (two held doors + a patrol; echo B reaches its switch through echo A's door; budget 2)
- `Levels/room-09.json` — "Contested" (thread a patrolled centre, two held necks; **regrown 5×7 with a stall pocket**, D-039; budget 2)
- `Levels/room-10.json` — "Capstone" (everything at once; switch-dependency keeps it to two echoes; budget 2)
- **Phase 3.01 — the full campaign (rooms 11–20), same locked v1 format:**
- `Levels/room-11.json` — "Crossroads" (two held gates in series, the echoes time-contest the crossings; budget 2)
- `Levels/room-12.json` — "Both at Once" (one **AND-door**, `heldBy:[s1,s2]` — both echoes must hold it at once; budget 2)
- `Levels/room-13.json` — "Wide Gate" (one switch opens a **two-tile** gate; a patrol makes the low straight run fatal, so cross on the top tile; budget 1, see report §4)
- `Levels/room-14.json` — "Twin Patrols" (one held door, **two** patrols sweep the corridor; dip to the parallel row and time it; budget 1)
- `Levels/room-15.json` — "The Long Hold" (a long-period patrol; one echo settles early and holds through the whole circuit — patience; budget 2)
- `Levels/room-16.json` — "Three" (**budget 3** — three held gates in series, four live timelines; interior start)
- `Levels/room-17.json` — "Threadneedle" (AND-door **+ two** patrols — cooperation and timing together; budget 2)
- `Levels/room-18.json` — "Clockwork" (AND-door + single gate + a column-clocking patrol; **budget 3**)
- `Levels/room-19.json` — "Lattice" (the hardest: a **multi-cell AND-door** {two tiles, two switches} + two out-of-phase patrols; **budget 3**)
- `Levels/room-20.json` — "Coda" (a gate·patrol·gate·patrol clockwork, AND-door + single + two patrols; **budget 3**; was the campaign finale through Part 3 — now the end of the Part-3 band, D-065)
- **Phase 4.02 — the strategic / relocating-echo band (rooms 21–25), same locked v1 format (D-069):**
- `Levels/room-21.json` — "Relay" (one echo holds switch A, **waits**, then relocates to switch B — one self, two doors; **budget 1**; the wait makes the single-echo relay possible)
- `Levels/room-22.json` — "Two Relays" (two relocating echoes interleaved — echo 1 covers doors 1 & 3, echo 2 doors 2 & 4; **budget 2**, no enemy)
- `Levels/room-23.json` — "Patrol & Relay" (a relay echo + a static hold + a patrol bouncing the open column; read the patrol while the relay works; **budget 2, one enemy**)
- `Levels/room-24.json` — "Hold & Hand-off" (a 2-switch **AND-door** held by both echoes at once, then one echo hands off to a later door; **budget 2**)
- `Levels/room-25.json` — "Clockwork" (oversized capstone: a **three-switch AND-door**, two of those echoes then relay to later doors, **two out-of-phase patrols**; **budget 3** — the band's milestone, bites like room 20)
- **Phase 4.04 — the teleport band (rooms 26–30), the v2 `portals` format (D-073):**
- `Levels/room-26.json` — "Threshold" (one pad pair joins two walled regions; an echo holds switch A in A, present-you **portals** to B and exits the door A holds; **budget 1** — the portal, clean)
- `Levels/room-27.json` — "Two Rooms" (an echo **teleports** to the far region to hold s2 while another holds s1 near; two door-pairs; present-you crosses d1, portals, crosses d2; **budget 2**, no enemy — echoes traverse portals)
- `Levels/room-28.json` — "Portal & Patrol" (a patrol bounces the partner-pad column, so the teleport **landing** is timed — danger where you land; **budget 2, one enemy**)
- `Levels/room-29.json` — "Relay Across" (wait + teleport: one echo holds sA, **waits**, relocates **through a portal** to sB; a second echo statically opens the portal approach; **budget 2** — the hard pre-capstone)
- `Levels/room-30.json` — "Junction" (oversized capstone: a hub with **three pad pairs** to three isolated switch-spokes, three echoes teleport out to hold an **AND-door across three regions**, **two out-of-phase patrols**; **budget 3** — rehearses room 36's portal density)
- **Phase 4.06 — the mirror band (rooms 31–35), the `mirror` format extension (D-077):**
- `Levels/room-31.json` — "Symmetry" (symmetric halves in perfect lockstep; **one** mirror echo rests on both mirrored switches at once, then both bodies walk home; **budget 1**, 8×4 — the two bodies, clean)
- `Levels/room-32.json` — "Break Symmetry" (the exits are NOT mirror-aligned and reflected controls preserve the bodies' column-sum, so lockstep can never win — the one-sided wall at (2,2) **desyncs** the pair; **budget 1**, 8×4)
- `Levels/room-33.json` — "Cross-Half Hold" (two asymmetric switches, one per half, hold BOTH doors as a **cross-half AND** — no single echo can rest on both and a live holder can't also be home: two folds; **budget 2**, 8×4)
- `Levels/room-34.json` — "Mirror & Patrol" (two out-of-phase patrols sweep row 3, one per half — the echo **recordings** are what must be timed, steering two bodies at once; cross-half AND-doors; **budget 2, two enemies**, 8×5 — the hard pre-capstone)
- `Levels/room-35.json` — "Reflection" (**the CAMPAIGN FINALE**, D-065: 10×6, three switches — sA behind the period-2 patrol (the recording is timed), sB deep right, sC reachable only by **desync** (its mirror cell is a wall) — holding both exit doors as a 3-switch cross-half AND; patrol h1 owns the dash's first window so the finish is timed too; **budget 3, two enemies**)
- *(`Levels/.gitkeep` removed in 1.06; no `.gitkeep` here — outside `ECHO/`, so no D-012 collision either way)*

## Tests (`ECHOTests/`)
- `ECHOTests/ECHOTests.swift` — `@MainActor` XCTest coverage of the move model, the fold/replay suite, the collision/restart suite (1.03–1.05), and the 1.06 suite (walls; switch/door + echo-holds-switch canonical solve; hazard land-on / cross-paths-swap / loop-non-loop-stationary; win + input lock; collision-before-win; echo-budget refusal; JSON decode; level-load reset), **plus the 1.07 suite**: step-back rollback; turn-0 no-op + no-un-fold guard; repeated step-back to turn 0 (echoes preserved); step-back refused while won; step-back-then-branch-move (invariant `turn == currentRun.count`); derived-world rollback (switch+door + hazard + echo → turn-1 values); reset-run preserving echoes incl. reset-while-won. No prior test needed re-pathing (1.07 changes no move/fold/collision/win semantics)
- `ECHOTests/SettingsGuidanceTrailTests.swift` — **Phase 2.06** `@MainActor` additive suite for the phase's pure/persisted helpers: `SettingsStore` defaults + the `UserDefaults` round-trip + write-through; `GuidanceController` room→hint mapping, the verbatim guidance strings (now **four** — the `blockedByGhost` assertion dropped in Phase 3.03 with the string's removal), and the seen-once gate (fires once ever, persists across instances) + the `enterRoom`/`showEaten` message triggers; and `Echo.upcomingCells` (remaining-path list, empty when exhausted, no consecutive duplicates). Each test uses its own isolated `UserDefaults` suite. No engine rule touched
- `ECHOTests/RoomSolvabilityTests.swift` — **Phase 1.08** `@MainActor` per-room suite (D-034): one solvability test per `room-01`…`room-10` that loads the JSON via `LevelLoader` (source-tree fallback), replays a verified reference solution through the real `GameState` (asserting every move lands, each fold banks one echo within budget, the final run reaches `exit` with `hasWon`), plus the negative tests for rooms 04/06/07 (the named naive run dissolves and does not win) and a hazard-trace test (each patrol's one-period trace + period wrap matches the documented path). **Phase 3.01:** extended with one solvability test per `room-11`…`room-20` (same `assertSolves` helper, verified reference solutions within budget), four new negative tests (room-12 one-echo leaves the AND-door shut; rooms 13/14/17 naive straight runs die), and the hazard-trace test extended via a new `traceAt(id, index, …)` helper to cover **every** new hazard (both hazards in the two-patrol rooms 14/17/19/20). **Phase 4.02:** `assertSolves` (and a new `replayRun` helper) now treat a **repeated cell as a wait** (`GameState.wait()`), so a reference run can dwell in place; added one solvability test per `room-21`…`room-25` (the strategic-echo band, verified through the real engine within budget), two negatives (rooms 23 & 25 — a naive run that dies to the patrol), and the hazard-trace test extended to room-23's patrol and room-25's two out-of-phase patrols. **Phase 4.04:** `replayRun` now also recognises a **teleport** step (a non-adjacent pair whose `resolveLanding` through the board's `padMap` matches the next cell); `assertSolves` gains a `portals:` structural count; added one solvability test per `room-26`…`room-30` (the teleport band), two negatives (rooms 28 & 30 — a mistimed hop / a straight rush into a patrol), and the hazard-trace test extended to room-28's and room-30's patrols. **Phase 4.06:** adds the mirror-aware helpers `replayMirrorRun` (replays an INPUT stream — `.stay` = wait — through the real `MirrorGameState`, asserting every input commits and the turn advances by exactly one, so a both-blocked no-op or a mid-run death fails; a deliberately-blocked single body — the desync — still passes) and `assertSolvesMirror` (structural spec incl. the `mirror` block, even width, both starts/exits; per-run replay; each fold banks one mirror echo; BOTH bodies on their own exits with `hasWon`, within budget); one solvability test per `room-31`…`room-35`, two negatives (room 34 — a mistimed echo recording dies to patrol h2; room 35 — the dash one beat early dies to patrol h1), and the hazard-trace test extended to rooms 34 & 35's four patrols
- `ECHOTests/EchoRunTests.swift` — **new (Phase 3.02)** `@MainActor` suite for the arcade engine `EchoRunState`, driven through its real API (no re-implementation): centre start on a 9×9; spawn cadence fires at turns 8/16/24 (default period); each shadow retraces the player's exact path at given turns (lagging by its spawn offset); the edge-swipe **stall** (player holds, turn advances, shadows step, recording unchanged); **land-on** death (a trailing shadow catches a stalled head, turn 12) and **cross-paths/swap** death (turn 3, with an injected small `spawnPeriod`), each with the right score; score = turns survived incl. stalls; input locked after death; a clean `reset()`; `pendingDeath` agrees with the commit; and the Echo Run **high-score round-trip + keep-best** through `SettingsStore`. **+15 methods** over the 3.01 tip. **Phase 4.01** one-line fix: `testEveryMoveFromCentreIsARealStepNotAStall` now sweeps the four movement directions explicitly instead of `Direction.allCases` (which gained `.stay`, a campaign-only verb Echo Run never issues — D-067)
- `ECHOTests/NavigationTests.swift` — **new (Phase 3.03)** `@MainActor` suite for the testable navigation logic (brief §Tasks 12): the `Campaign` catalog/order helpers (20 rooms in zero-padded order; `number`/`contains`; `next(after:)` follows order, **nil at the final-room boundary** and on unknown ids; a full-chain walk visits the campaign once then stops; `firstUnsolved` by play-order incl. all-solved → nil); the **solved-state persistence** (`SettingsStore` defaults empty; `markSolved`/`isSolved`; **idempotent** mark; round-trip across store instances; write-through to the key; independence from the four `Bool` prefs + high score); and the **no-gating invariant** (solved rooms stay selectable; the selectable set is constant across solved-state, only the `firstUnsolved` accent hint varies). **+19 methods** (D-060/D-061). **Phase 4.02** updates the catalog assertions to **25 rooms** (`room-01…room-25`, last `room-25`), recognises `room-21…room-25` in `contains`, and moves the final-room boundary (`next(after: "room-25") == nil`; `next(after: "room-20") == "room-21"`). **Phase 4.04** updates the catalog to **30 rooms** (`room-01…room-30`, last `room-30`), recognises `room-26…room-30`, and moves the boundary again (`next(after: "room-30") == nil`; `next(after: "room-25") == "room-26"`). **Phase 4.06** updates the catalog to **35 rooms** (`room-01…room-35`, last `room-35` — the campaign finale), recognises `room-31…room-35` (and rejects `room-36`), and moves the boundary to the finale (`next(after: "room-35") == nil`; `next(after: "room-30") == "room-31"`, `next(after: "room-34") == "room-35"`)
- `ECHOTests/FrameStatsTests.swift` — **new (Phase 3.04 / D-063)** `@MainActor` suite for the pure performance-overlay math (`FrameWindow`/`FrameStats`): empty-window zeros (fps guarded, not 1/0); instantaneous/average/worst/fps over a known interval sequence; capacity eviction + clamp-to-≥1; non-finite/non-positive samples ignored; `droppedCount` threshold + tolerance (incl. default 1.5 and zero/negative-input guards); window `reset`; and the `summary` ms/count composition. Frame intervals chosen as exact binary fractions so equalities need no `accuracy:` tolerance. **+12 methods** (140 total)
- `ECHOTests/WaitActionTests.swift` — **new (Phase 4.01)** `@MainActor` suite driving the **real** `GameState`/`Echo`/`Direction` for the wait primitive (D-066/D-067): `.stay` is a zero-offset fifth direction the tap rule never produces; `move(.stay)` is a no-op; `wait()` advances the turn / appends one `.stay` / leaves the player put / reports `.stepped`; a wait is refused while won; a wait is **fatal** when a mover lands on the held tile (`.died` + restart); `stepBack` pops a `.stay` and unwinds a mixed run to turn 0 with echoes intact; `fold` banks a run with a `.stay` and the echo replays the dwell; and the headline **dwell-then-relocate proof** — one echo holds switch A, waits, relocates to switch B, opening two doors at two times for present-you to win (with a "door A shut without the echo" sub-assertion). **+8 methods**
- `ECHOTests/TeleportTests.swift` — **new (Phase 4.03)** `@MainActor` suite driving the **real** `GameState`/`Echo`/`Level` for the teleport mechanic (D-070–D-072): the shared `resolveLanding` rule (zero-offset `.stay` never jumps; partner never re-fires; empty-pads identity); a move onto a pad lands on the partner that turn (recording the input direction); partner-no-refire + `.stay`-on-pad; step-off-and-back teleports again; an echo replaying a step onto a pad **teleports and holds a far-region switch** (pad-aware `isCellHeld`); collision evaluated at the landing (against a hazard **and** an echo); a blocked resolved landing (closed door / wall) is a no-op; `stepBack` over a teleport; the **empty-pads regression**; level-format-v2 decode with/without `portals` + a bidirectional `padMap`; and the headline **wait + teleport relay** — one echo holds switch A, waits, then relocates **through a portal** to switch B in the other region, opening two doors at two times so present-you wins. **+13 methods**
- `ECHOTests/MirrorTests.swift` — **new (Phase 4.05)** `@MainActor` suite driving the **real** `MirrorGameState` (D-074–D-076): reflected controls (up/down move both the same; left/right oppositely, each stream recording its own step); `move(.stay)` refused; **partial movement** (a one-sided wall desyncs the bodies, the blocked body recording `.stay`; both-blocked ⇒ a no-op that advances nothing and leaves `lastMoveOutcome` untouched); centerline **confinement** (inward from the two innermost columns is a no-op); the wait holds both bodies and appends `.stay` to both streams; `fold()` banks a two-body echo within budget (refused empty/at-cap); a mirror echo **replays both bodies verbatim** incl. the blocked turn's reproduced `.stay` and exhausted standing; **cross-half hold** (a left-half switch opens a right-half door, read at the pre-step turn) and a **cross-half AND-door** (opens iff both halves' switches held; one folded mirror echo holds both at once); per-half **death** (ONLY the left / ONLY the right body touching an echo-body dissolves both — run restarts, echoes persist; hazard land-on; the live cross-paths swap; a fatal wait); the **both-home win** (one body home is not a win; a wall-held body counts as sitting on its exit; input locks after); `stepBack` pops both streams across a desynced turn (refused at turn 0); the additive `mirror` block decodes (and its absence decodes exactly as before); and the **headline proof** — desync past a one-sided wall → fold one mirror echo resting on two non-mirrored switches → the cross-half AND-door held open → both bodies home the same turn, within budget 1. **+21 methods**

## Design handovers & docs (`docs/`)
- `docs/design-handovers/Part-2-Phase-01-Handover.md` — the locked Phase 2.01 visual design (every colour hex, geometry pt, opacity, motion curve/duration); the source of truth implemented by Phase 2.02
- `docs/feel-audit.md` — **new (Phase 3.04).** The action→feedback checklist for the owed on-device session: every player-facing action in both modes mapped to its visual/audio/haptic channels, each marked **WIRED in code** vs **— none**, cross-referenced to the handover §/decision that governs it, with a top **"Gaps found"** section (G1–G7: blocked-move/refused-fold have no feedback — `Motion.denyShake` defined-but-unwired; reset/step-back silent; Echo Run spawn has no cue; stall is haptically identical to a step; menu/retry taps silent; game-over reveal has no pre-delay) and an **OWED-on-device** list. Documents gaps; invents none
