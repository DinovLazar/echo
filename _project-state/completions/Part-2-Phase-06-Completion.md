# Part 2 · Phase 06 · Code — Settings, persistence, the echo-trail aid & the guidance microcopy

**Date:** 2026-06-28 · **Outcome (one line):** ECHO now has a real, persisted Settings screen (Invert / Sound / Haptics / Echo trail), the optional echo-trail upcoming-path aid, and the quiet guidance-microcopy system — the "feel" layer is content-complete.

> Filed at the close of Phase 2.06. Factual; written so a non-developer can follow it. Verified against actual command output, not memory. The standing device-only items are carried forward honestly, **not** marked done.

## 1. What shipped (plain language)
The four design preferences finally have a real home: a **Settings screen** (reached from a temporary gear button in the debug bar) with four toggles — **Invert mode**, **Sound**, **Haptics**, **Echo trail** — that **save to the device and survive a relaunch**. Flipping a toggle takes effect immediately: the board flips palette, the audio and haptics turn on/off, the trail appears or fades. Two new on-board aids landed: the **echo trail** (an optional faint dotted preview of where each grey echo is about to walk, off by default) and the **guidance microcopy** (a few quiet words — `swipe to move`, `fold to keep the door open`, `beware — it bites` shown once each the first time they matter, and `you got eaten` on every death). It is all presentation: no game rule, level, or existing test changed.

## 2. Definition of Done
- ✅ **`phase-2.06-settings` cut from the 2.05 tip; all work on it; no `DEVELOPMENT_TEAM` in any commit; no `.gitkeep` added inside `ECHO/`/`ECHOTests/`.** Evidence: `git checkout -b phase-2.06-settings phase-2.05-haptics`; the `DEVELOPMENT_TEAM` line stays unstaged (working-tree only); new dirs are `ECHO/Settings/`, `ECHO/Guidance/` (no `.gitkeep`). **Reconciliation:** `main` HEAD == `phase-2.05-haptics` tip (`d968e84`); `git log main..phase-2.05-haptics` is empty, so 2.02–2.05 are **already merged to `main`** — the brief's "unmerged branch stack" framing is stale (see §4). Branching from the 2.05 tip is therefore identical to branching from `main`; the brief is honoured literally.
- ✅ **Presentation-only honoured.** Evidence: `git diff` touches no engine rule — `GameState.swift` unchanged; the only model edit is the additive pure `Echo.upcomingCells`. No `Levels/*.json` changed; no existing test changed/deleted (only `ECHOTests/SettingsGuidanceTrailTests.swift` added). `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` untouched. Touching an echo is still a death — no preventive block added (the 5th string stays unwired, D-052).
- ✅ **`SettingsStore` persists four prefs with the documented defaults (invert off, sound on, haptics on, echo-trail off).** Evidence: `ECHO/Settings/SettingsStore.swift`; `testSettingsDefaultsWhenNothingPersisted` + `testSettingsPersistAcrossStoreInstances` + `testSettingsWriteThroughToDefaultsKeys` pass in the harness run.
- ✅ **`SettingsView` shows the four toggles, palette-styled, as a `.sheet` from a gear control that replaces the debug Invert button; rest of the bar unchanged.** Evidence: `ECHO/Settings/SettingsView.swift`; `ContentView` debug bar — the gear replaces the old `Invert` button, Fold/Step back/Reset run/Clear/Next/readout/Solved are byte-for-byte as before. (Visual rendering unseen in CLT — carried, §7.)
- ✅ **Invert toggle flips the live palette and persists; the debug bar no longer has its own invert flip.** Evidence: `theme` is derived from `settings.invertEnabled`; `grep themeMode ECHO/` → none.
- ✅ **Sound/Haptics toggles drive `audio.isEnabled`/`haptics.isEnabled` live and persist; both initialised from the persisted values at launch.** Evidence: `ContentView.task` sets both from `settings` before `start()`/`prepare()`; `.onChange(of:)` keeps them synced. (Sensation unverifiable in CLT — carried.)
- ✅ **Echo-trail aid renders per §8/§6f (3 pt dots, 8 pt spacing, `echo.base` @ 0.40, along each echo's upcoming-path cells, beneath the player, hit-testing off); reveals with `Motion.trailReveal` + 8 ms/dot stagger on; fades 150 ms `curve.easeIn` off; nothing when off; no gameplay change.** Evidence: `BoardView.echoTrail`/`trailDots`/`EchoTrailLayer`; tokens in `BoardMetrics`; mount/unmount via `.task(id: showEchoTrail)`. (Motion unseen in CLT — carried.)
- ✅ **Guidance microcopy renders per §6 — five strings verbatim, SF Pro Text 15 pt `.medium` +0.2 tracking, `text.guidance`, no box/shadow, lower-third (≥24 pt above bottom, upper-third fallback), opacity-only fade-in 200 ms easeOut → linger (2200/1600 ms) → fade-out 350 ms easeIn.** Evidence: `GuidanceOverlay`; `testGuidanceStringsAreVerbatim` pins all five exactly. (Fades/placement unseen in CLT — carried.)
- ✅ **One-time hints fire once ever (persisted) on room-01 / room-03 / room-06; `you got eaten` on every death; `you can't go there — your ghost is` ships unwired with an explanatory comment.** Evidence: `GuidanceController` mapping + seen-once gate (tests: `testConsumeHintFiresOnceEver`, `testSeenPersistsAcrossControllers`, `testEnterRoomPresentsHintOnceThenStaysSilent`); `BoardView.triggerDeath` → `guidance.showEaten()`; `GuidanceFeedback.blockedByGhost` present with a deferral comment.
- ✅ **New additive tests cover the pure helpers; the full existing suite still passes.** Evidence: harness run → **80 methods / 781 assertions, 0 failures** (prior 67/734 + **13/47** new). Settings round-trip + defaults, the guidance seen-once gate + verbatim strings + triggers, and the echo upcoming-path list are all covered.
- ✅ **App type-checks clean under the app regime; new value type crossing a `nonisolated` requirement is `nonisolated` (D-040); new token constants additive.** Evidence: `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -sdk <MacOSX.sdk>` → **exit 0**; the only warnings are the 3 pre-existing AudioManager macOS-27 deprecations (→ 0 at `-target arm64-apple-macosx14.0`). `Echo.upcomingCells` is on the existing `nonisolated` value type; no regime change needed.
- ✅ **`ECHO-Decisions.md` has D-050, D-051, D-052 appended verbatim (D-049 confirmed highest first).** Evidence: `grep '^### D-0' ECHO-Decisions.md | tail` → …D-049, D-050, D-051, D-052.
- ✅ **`current-state.md`, `file-map.md`, `00_stack-and-config.md` synced.** Evidence: see §6.
- ⚠️ **A PR into `main` is open.** Branch pushed and PR opened at the end of the phase (see §5 for the URL/hash); the work is on `phase-2.06-settings`.

## 3. Decisions I made during this phase
- **Stale "unmerged branch stack" framing corrected (reconciliation, no new decision needed).** The brief's DoD says to carry forward "2.02–2.06 are still an unmerged branch stack awaiting an in-order merge into `main`." Reality: `main` == `phase-2.05-haptics` tip (`d968e84`), and `git log main..phase-2.05-haptics` is empty — 2.02–2.05 are **already on `main`**. Per CLAUDE.md (live code wins) and the prior-phase memory that ECHO briefs can lag the repo, I branched from the 2.05 tip (identical to `main`) and corrected the framing here rather than carry a false item. The only outstanding PR is 2.06's. **Decisions entry needed: NO** (a repo-state reconciliation, surfaced to the owner; the three pre-authored decisions D-050/051/052 cover the actual scope choices).
- **`GuidanceMessage` carries a `category` (hint/feedback), not a raw linger duration; the overlay maps category → `Motion.Span` linger.** This keeps `GuidanceController` SwiftUI-free (Foundation + Observation only), so the room→hint mapping and the seen-once gate are unit-testable in the headless model harness. The presentation timing lives where the rest of the §6 motion lives (`GuidanceOverlay`). **Decisions entry: NO** (an internal factoring; the brief's behaviour is met exactly).
- **`SettingsView` uses hand-built `Binding(get:set:)` and a passed `onClose` closure instead of `@Bindable`/`@Environment(\.dismiss)`.** Same observable single-source-of-truth behaviour, but it keeps the file clear of extra property-wrapper macros (the CLT type-check substitutes those) and of iOS-only navigation API, so it type-checks under the no-Xcode harness and against the macOS SDK exactly like the rest of the app. **Decisions entry: NO.**
- **Echo-trail layer mounts only while on or fading (a `trailMounted` flag driven by `.task(id: showEchoTrail)`), with per-dot opacity animated by an on-appear `revealed` flip.** This honours "Off = nothing rendered" literally (the layer truly unmounts after the 150 ms fade) and makes the staggered reveal animate on appearance (a plain `.animation(value:)` does not animate an initial mount). **Decisions entry: NO** (an implementation detail of the specced behaviour).

## 4. Deviations from the brief / spec
- **The carried-forward DoD item "2.02–2.06 are still an unmerged branch stack" is factually stale** and was not carried as-is — 2.02–2.05 are already merged to `main` (see §3). 2.06's PR is the only one outstanding. This is the one place the brief and the live repo disagreed; live code wins (CLAUDE.md).
- **No other deviation.** All in-scope work shipped; the explicitly out-of-scope items (the §8.1 deny-shake, a preventive block of moving into an echo, the 5th guidance string's trigger, any real menu/title/level-select/win overlay, screen polish) were left out by design (D-052/D-037).

## 5. Changed files / deliverables
- **Code (new):** `ECHO/Settings/SettingsStore.swift`, `ECHO/Settings/SettingsView.swift`, `ECHO/Guidance/GuidanceController.swift`, `ECHO/Guidance/GuidanceOverlay.swift`, `ECHOTests/SettingsGuidanceTrailTests.swift`.
- **Code (edited, additive / presentation-only):** `ECHO/App/ContentView.swift` (owns `SettingsStore` + `GuidanceController`; derives palette from settings; manager sync; gear + sheet; `enterRoom`), `ECHO/Views/BoardView.swift` (echo-trail layer + `GuidanceOverlay` + `showEaten` on death; two new injected params), `ECHO/Models/Echo.swift` (additive `upcomingCells`), `ECHO/Theme/Theme.swift` (additive trail tokens), `ECHO/Theme/Motion.swift` (additive `trailFadeOut` + guidance/trail `Span`s).
- **Branch / PR / commit:** branch `phase-2.06-settings` (from the 2.05 tip `d968e84`); commit hash + PR URL recorded in a follow-up docs commit.
- **Design:** implements `docs/design-handovers/Part-2-Phase-01-Handover.md` §6 (typography + guidance) and §8 (echo-trail aid); reads §0/§1a/§1b/§7. No new handover authored.
- **Ops / manual:** none. No secrets. The `DEVELOPMENT_TEAM` line Xcode writes on build is left unstaged.

## 6. State updates done (code phases)
- [x] `current-state.md` overwritten to reflect what actually shipped (Phase 2.06 snapshot).
- [x] `file-map.md` updated for every add/edit (the four new source files, the new test file, and the edited Echo/Theme/Motion/ContentView/BoardView lines; the `ECHO/Settings/` + `ECHO/Guidance/` folders; the `HapticsManager.swift` line that was still showing the folder as reserved).
- [x] `00_stack-and-config.md` appended (2026-06-28 Phase-2.06: still zero external packages; new files; additive token constants; the four `settings.*` + per-hint `guidance.seen.*` UserDefaults keys introduced; CLT verification).

## 7. Risks, follow-ups, what the next phase needs to know
- **Everything 2.06 draws is unseen in the CLT-only env.** The Settings sheet and the live effect of each toggle, the echo-trail dots/reveal-stagger/fade in both palettes, and the guidance microcopy's fades + lower/upper-third placement are all unverified visually — they need an on-device `⌘R` feel pass (in both Light and Invert). The logic underneath (persistence, defaults, the seen-once gate, the upcoming-path list, the verbatim strings) **is** verified by the harness.
- **Standing device-only items still owed (carried, NOT done here):** no on-device `⌘R` or `⌘U` has run; the audio has not been heard and the haptics not felt (2.04/2.05); the D-025 `Levels/` runtime bundling is unconfirmed; the exact Xcode 27 / iOS SDK build numbers (owed since 1.02) are unpinned.
- **Adversarial 5-lens review** (spec / presentation-only / concurrency / wiring / render-math), each finding then adversarially verified. 3 lenses found nothing; **2 findings confirmed and both fixed before the commit**: (1) *low* — the four new guidance value types (`GuidanceHint`/`GuidanceFeedback`/`GuidanceCategory`/`GuidanceMessage`) were not marked `nonisolated`/`Sendable` like every other value type (D-013) → marked them; (2) *medium* — the guidance caption could teleport the board height if the player crossed the lower-third boundary while a message was visible (the placement was read live), violating §6's "the message slides nowhere — opacity only" → the overlay now **captures the anchor third once when the message appears** and holds it. Re-verified after the fixes: type-check exit 0, suite 80/781 green.
- **For the next phase:** the three switch points are now driven by `SettingsStore` (the real menu in Part 3 should reuse it, not re-source the prefs); the gear + debug bar are still throwaway and come out when Part 3 builds the menu; the 5th guidance string and the §8.1 deny-shake remain unwired pending the block-vs-bite gameplay decision (D-052).

## 8. What's now possible that wasn't before
The owner can set and keep his preferences, switch on the trail when a board gets busy, and the game teaches its own verbs in a few quiet words — Part 2's "feel" layer is content-complete, leaving only the on-device pass before Part 3.
