# 00_stack-and-config.md  (append-only)

> A log of stack and config decisions with **exact pinned versions**. Append a dated entry whenever a dependency or tool is added or upgraded; never rewrite a past entry. This is what keeps the build reproducible. Pin exact versions (`x.y.z`), never "latest" or caret ranges — silent drift is undebuggable for a non-developer owner.

---

## 2026-06-24 — Planning lock (pre-scaffold)

The locked stack for ECHO, as decided during intake and planning. Versions reflect what's current as of this date; Code confirms and re-pins the exact installed versions during Phase 1.01 and appends a correction here if anything differs.

- **Language:** Swift 6.4 (bundled with Xcode 27)
- **Build tool / IDE:** Xcode 27 (currently the WWDC 2026 beta; public release expected ~Sept 2026)
- **Host OS to build on:** macOS 26.4+ (Tahoe) or macOS 27, Apple silicon only
- **UI foundation:** SwiftUI (system framework, tracks the SDK)
- **Deployment target (min OS):** iOS 17.0
- **Dependency manager:** Swift Package Manager (no external packages at v1 — target zero)
- **Animation:** SwiftUI built-in (system)
- **Particles:** SwiftUI Canvas (system); thin SpriteKit layer only if needed (system)
- **Audio:** AVAudioEngine (system)
- **Haptics:** Core Haptics + UIFeedbackGenerator (system)
- **Persistence:** UserDefaults (system) for solved-levels + Echo Run high score
- **Level data:** plain JSON files in `Levels/` (no library)
- **Tests:** XCTest (system)
- **Source control:** Git + GitHub `DinovLazar/echo` (public), single branch `main`
- **Distribution:** free Apple ID signing + SideStore (no paid Apple Developer Program)

**Notes:**
- "System" frameworks (SwiftUI, AVAudioEngine, Core Haptics, XCTest, etc.) are versioned by the iOS SDK / Xcode, not pinned separately. The number that matters is the **deployment target (iOS 17.0)** and the **Xcode/Swift version** above.
- No third-party packages are planned for v1. If one is ever added, append an entry here with its exact pinned version and the reason (and log the choice in `ECHO-Decisions.md`).
- Building on a beta toolchain is a known, accepted risk (D-009). If Xcode 27 / iOS 27 advances to a new beta or to public release, append the new versions here.

---

## 2026-06-24 — Phase 1.01 scaffold (confirm + pin)

The Xcode project was created during Phase 1.01. Confirmed/observed versions and config choices:

- **Swift toolchain (confirmed installed):** `Apple Swift version 6.4 (swiftlang-6.4.0.20.104 clang-2100.3.20.102)` — matches the planned Swift 6.4 lock.
- **Swift language mode (build setting `SWIFT_VERSION`):** `6.0` (the Swift 6 language mode), set at the project level for both Debug and Release. The trivial scaffold code (App + placeholder View + one test) has no concurrency surface, so Swift 6 mode is safe; flip to `5.0` only if a later phase hits strict-concurrency friction it can't resolve.
- **Deployment target:** `IPHONEOS_DEPLOYMENT_TARGET = 17.0` (unchanged from plan).
- **Bundle id:** `com.dinovlazar.echo` · **Display name:** `Echo` (via `INFOPLIST_KEY_CFBundleDisplayName`; `GENERATE_INFOPLIST_FILE = YES`, no standalone Info.plist).
- **Project-file format:** `ECHO.xcodeproj/project.pbxproj` hand-authored using Xcode's **file-system synchronized groups** (`PBXFileSystemSynchronizedRootGroup`), `objectVersion = 77`. No project-generation tool (no XcodeGen) was added — the zero-package goal is intact and there is no build-time tool dependency.
- **Packages:** still **zero** Swift Package dependencies.
- **Source control:** Git initialised, single branch `main`, `origin = https://github.com/DinovLazar/echo` (public).

**Environment caveat (important for the next session):** the machine that scaffolded the project had **only the Xcode Command Line Tools installed, not full Xcode** (`xcodebuild`/`simctl` unavailable). The project therefore could **not** be compiled or run in the Simulator during Phase 1.01. Versions above are confirmed from the Swift CLI; the **Xcode 27 / iOS SDK build numbers must be pinned here by the first session that opens the project in full Xcode** (Phase 1.02), along with confirmation that ⌘R builds and runs.

---

## 2026-06-24 — Phase 1.02 first device install (Code track)

The hello-grid placeholder was added. No dependency, tool, or version change this phase — recording the config facts the brief asked to capture:

- **Toolchain (unchanged):** Swift 6.4 confirmed from the CLI (`swift --version` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`). Deployment target unchanged at `IPHONEOS_DEPLOYMENT_TARGET = 17.0`. No Swift Package dependencies added (still zero).
- **Signing:** `CODE_SIGN_STYLE = Automatic` on **both** targets (`ECHO` and `ECHOTests`) — "Automatically manage signing" is on. **No `DEVELOPMENT_TEAM` is set** in `project.pbxproj` (left for Lazar to pick his personal Apple ID team at device-install time). Already true since 1.01; confirmed unchanged here.
- **Verification performed in this environment:** `swiftc -typecheck -parse-as-library` of `ContentView.swift` + `HelloGridView.swift` against the **macOS** SDK (`/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk`) → **exit 0 / clean** (the `#Preview` macro is excluded because its plugin ships only with full Xcode, not CLT). This is a syntax/type smoke test, **not** an iOS Simulator build.
- **Still NOT verified here (no full Xcode — same caveat as 1.01):** the iOS Simulator build, the on-device ⌘R run, the `⌘U` test run, and a real Simulator/device screenshot. **Pinning still owed:** the exact **Xcode 27 / iOS SDK build numbers** must be appended by the first session that opens the project in full Xcode (Lazar's Mac).
- **Build-config fix during Lazar's first Xcode build (D-012):** the first compile failed with `Multiple commands produce …/ECHO.app/.gitkeep` (1 error + 2 "duplicate output file" warnings). **Cause:** Xcode's file-system synchronized groups (`PBXFileSystemSynchronizedRootGroup`) treat `.gitkeep` as a **bundle resource** and copy it — the three identically-named `ECHO/{Models,Audio,Haptics}/.gitkeep` files all map to `ECHO.app/.gitkeep` and collide. **Fix:** removed those three `.gitkeep` files (the empty folders are now untracked until a real source file lands; `.gitkeep` files **outside** any synchronized group — `Levels/`, `docs/design-handovers/` — are safe and were kept). **Rule for this repo:** never place a `.gitkeep` (or any duplicate-named throwaway file) inside `ECHO/` or `ECHOTests/`; to keep an in-target folder, add a real source file or exclude the keeper via a `PBXFileSystemSynchronizedBuildFileExceptionSet`. Corrects the Phase 1.01 report's incorrect assumption that synchronized groups ignore dotfiles.
- **First successful build verified (Lazar's Xcode 27):** after the D-012 fix, the project builds, installs, and launches on the **iOS 27.0 Simulator, iPhone 17 Pro**. This is the first confirmed compile of the project (Phase 1.01 shipped unbuilt). **Still owed:** the exact **Xcode 27 / iOS SDK build numbers** — capture `xcodebuild -version` on the build Mac and pin them here (the Code environment can only confirm Swift 6.4 from the CLI). Physical-device install and a `⌘U` test run are still pending.

---

## 2026-06-24 — Phase 1.03 grid + move (Code track)

The first real model code and tests were added. **No dependency, tool, or version change this phase** — still Swift 6.4, zero Swift Package dependencies, deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`. Recording the config-relevant facts and how they were verified:

- **Actor-isolation regime (existing build setting, now load-bearing):** the app target's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (set since 1.01) makes unannotated declarations main-actor-isolated. Phase 1.03's model is written to that regime per **D-013**: `GameState` is `@MainActor @Observable`; the pure value types `GridCoordinate`/`Direction` are explicitly `nonisolated`/`Sendable`; the XCTest case is `@MainActor`. The **test target does not** set `SWIFT_DEFAULT_ACTOR_ISOLATION` (so it defaults to nonisolated) — the explicit annotations make the model correct under both.
- **Verification performed in this environment (CLT only):**
  - Models type-check clean under the app regime (`swiftc -typecheck -parse-as-library -default-isolation MainActor -sdk <MacOSX.sdk>`) **and** under the test regime (no flag) → exit 0 each.
  - Views + models type-check clean under the app regime → exit 0, after a smoke-test substitution for the two SwiftUI macros that the **CLT lacks** (`@State` → plain property; `#Preview` stripped). The production files retain both and compile in real Xcode.
  - A standalone executable built from the real model sources runs every XCTest scenario → **18/18 PASS**.
- **`-default-isolation MainActor`** is the `swiftc` frontend flag corresponding to the `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` build setting — recorded here for reproducible local type-checking in a no-Xcode environment.
- **Still NOT verified here (no full Xcode — same caveat as 1.01/1.02):** the iOS Simulator build, the on-device ⌘R run, and the **⌘U** XCTest run. **Pinning still owed:** the exact **Xcode 27 / iOS SDK build numbers** (paste `xcodebuild -version` on Lazar's Mac).

---

## 2026-06-24 — Phase 1.04 fold / record & replay (Code track)

The fold mechanic (`Echo` type, `GameState` record/fold/clear, echo rendering, debug bar) was added. **No dependency, tool, or version change this phase** — still Swift 6.4, **zero** Swift Package dependencies, deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo` all unchanged. Recording the config-relevant facts and how they were verified:

- **Concurrency regime unchanged (D-013, extended):** the new `Echo` value type follows the established discipline — `nonisolated struct Echo: Identifiable, Equatable, Sendable` (a `UUID` + `[Direction]`, both Sendable), so it is usable from any context like `GridCoordinate`/`Direction`. `GameState` stays `@MainActor @Observable`; the test case stays `@MainActor`. App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not.
- **Toolchain caveat refined:** under this Swift 6.4 CLT, SwiftUI's **`@State` is now an attached macro** (`SwiftUIMacros.StateMacro`) whose plugin ships only with full Xcode — so, like `#Preview`, it can't be expanded here. For the views-type-check smoke test, `@State` is de-macroed to a plain property and `#Preview` is stripped in a throwaway copy; the production sources keep both and compile in real Xcode. `import XCTest` also does not resolve under CLT (confirmed `no such module 'XCTest'`).
- **Verification performed in this environment (CLT only):**
  - Model (`GridCoordinate` + `Direction` + `Echo` + `GameState`) type-checks clean under the app regime (`swiftc -typecheck -parse-as-library -default-isolation MainActor -sdk <MacOSX.sdk>`) **and** under the test regime (no flag) → exit 0 each.
  - Views + model type-check clean under the app regime → exit 0 (with the `@State`/`#Preview` macro substitution above).
  - The verbatim `ECHOTests.swift` (minus its two CLT-unresolvable imports) type-checks against the model API under the test regime via a minimal **XCTest shim** (`XCTestCase` + the `XCTAssert*` used) → exit 0.
  - A standalone executable built from the **real** model sources (`main.swift` driving `@MainActor GameState` via `MainActor.assumeIsolated`) runs every test scenario (all 1.03 move checks + all 1.04 fold/replay checks) → **39/39 PASS**.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–1.03):** the iOS Simulator build, the on-device ⌘R run, and the **⌘U** XCTest run, plus a real screenshot. **Pinning still owed:** the exact **Xcode 27 / iOS SDK build numbers** (paste `xcodebuild -version` on Lazar's Mac).

---

## 2026-06-24 — Phase 1.05 collision + restart (Code track)

The collision rule and death restart (`GameState.playerCollides(...)`, `GameState.restartRun()`, `move(_:)` wiring) plus the collision test suite were added. **No dependency, tool, or version change this phase** — still Swift 6.4, **zero** Swift Package dependencies, deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. No `.pbxproj` edit (synchronized groups; no files added/renamed). Recording the config-relevant facts and how they were verified:

- **Concurrency regime unchanged (D-013):** the new collision predicate and `restartRun()` are plain methods on the `@MainActor @Observable GameState`; they read only `start`/`echoes` and the `nonisolated` value types, so nothing new crosses an isolation boundary. App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not.
- **Toolchain still CLT-only (confirmed):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode, but active developer directory '/Library/Developer/CommandLineTools' is a command line tools instance`; `simctl` absent. So the exact **Xcode 27 / iOS SDK build numbers remain unpinned** — still owed from 1.02, to be captured on Lazar's Mac. Swift CLI confirms `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`; macOS SDK present is `MacOSX27.0.sdk` (CLT).
- **Verification performed in this environment (CLT only):**
  - Model (`GridCoordinate` + `Direction` + `Echo` + `GameState`) type-checks clean under the app regime (`swiftc -typecheck -parse-as-library -default-isolation MainActor -sdk <MacOSX.sdk>`) **and** under the test regime (no flag) → exit 0 each.
  - Views + model type-check clean under the app regime → exit 0 (with the `@State`/`#Preview` macro substitution CLT requires).
  - The verbatim `ECHOTests.swift` (minus its two CLT-unresolvable imports) type-checks against the model API under the test regime via a minimal **XCTest shim** (confirms `playerCollides` and the new test methods compile) → exit 0.
  - A standalone executable built from the **real** model sources runs every test scenario (all 1.03 move checks + all 1.04 fold/replay checks + all 1.05 collision/restart checks) → **134/134 PASS**.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–1.04):** the iOS Simulator build, the on-device ⌘R run, and the **⌘U** XCTest run, plus a real screenshot of the death snap-back. **Pinning still owed:** the exact **Xcode 27 / iOS SDK build numbers** (paste `xcodebuild -version` on Lazar's Mac).

---

## 2026-06-25 — Phase 1.06 room contents, level data & win (Code track)

The puzzle contents (walls/switches/doors/hazards/exit/win/budget), the `Level`/`Hazard` models + `LevelLoader`, the level JSON, and the rendering/debug-bar additions were added. **No dependency or external-package change this phase** — still Swift 6.4, **zero** Swift Package dependencies, deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged.

- **One project-file change (`.pbxproj`) — Levels bundling (D-025):** a **third `PBXFileSystemSynchronizedRootGroup` (`Levels`, id `…B432`)** was added to the project's main group and to the **app target's `fileSystemSynchronizedGroups`**, so the repo-root `Levels/*.json` bundle as app resources (the same mechanism already used for the `ECHO` and `ECHOTests` source roots — `.json` files are auto-classified as bundle resources). The obsolete `Levels/.gitkeep` was removed (the folder now holds real files; it sits outside `ECHO/`, so no D-012 name-collision risk). **This edit was made blind — the CLT has no `xcodebuild` to validate it — and is flagged in `current-state.md` / the completion report for Lazar to confirm on first ⌘R** (the loader degrades to a bare board, not a crash, if bundling somehow fails).
- **Concurrency regime unchanged (D-013):** the new value types `Level`/`Switch`/`Door`/`Hazard` are all `nonisolated` + `Sendable` (+ `Decodable`), like `GridCoordinate`/`Direction`/`Echo`; `GridCoordinate` gained `Codable` and `Direction` gained a `String` raw value + `Codable`. `GameState`'s new members are plain methods/derivations on the existing `@MainActor @Observable` class. App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not.
- **Toolchain still CLT-only (confirmed):** `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`; `xcodebuild`/`simctl` absent. So the exact **Xcode 27 / iOS SDK build numbers remain unpinned** — still owed from 1.02, to be captured on Lazar's Mac. macOS SDK used for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk`.
- **Verification performed in this environment (CLT only):**
  - Full model (`GridCoordinate` + `Direction` + `Echo` + `Hazard` + `Level` + `GameState`) type-checks clean under the app regime (`-default-isolation MainActor`) **and** the test regime (no flag) → exit 0 each.
  - Views + model type-check clean under the app regime → exit 0 (with the `@State`/`#Preview` macro substitution CLT requires; `@State` swapped for a property wrapper with a `nonmutating set` to preserve SwiftUI's mutation semantics).
  - The verbatim `ECHOTests.swift` (its `import XCTest`/`@testable import ECHO` swapped for `import Foundation` + a minimal XCTest shim) type-checks against the model API under the test regime → exit 0.
  - A standalone executable built from the **real** model sources runs every test scenario (1.03 + 1.04 + 1.05 + all new 1.06: walls, switch/door, canonical solve, hazard land-on/swap/loop, win, collision-before-win, budget, JSON decode, level-load reset) **and** loads all three proof rooms from disk and plays their solutions (plus the naive-fail paths) → **172/172 PASS**.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–1.05):** the iOS Simulator build, the on-device ⌘R run, the **⌘U** XCTest run, a real screenshot, and that the **`Levels/` bundling edit** copies the JSON into the app bundle at runtime. **Pinning still owed:** the exact **Xcode 27 / iOS SDK build numbers** (paste `xcodebuild -version` on Lazar's Mac).

---

## 2026-06-26 — Phase 2.02 board look + motion (Code track)

The real visual design and step motion were added: a `Theme` colour-token layer (`ECHO/Theme/Theme.swift`), the named easing curves (`ECHO/Theme/Motion.swift`), and a full rewrite of `BoardView` to render every element to spec with sliding/squash motion. **No dependency, tool, or external-package change this phase** — still Swift 6.4, **zero** Swift Package dependencies, deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the two new files live under the existing `ECHO` file-system-synchronized root group and are picked up automatically (no target-membership edit needed).

- **System frameworks only (unchanged set):** SwiftUI, including `Animation.timingCurve` / `interpolatingSpring` for the curves and `keyframeAnimator(initialValue:trigger:content:keyframes:)` for the squash/anticipation. `keyframeAnimator` is iOS 17.0+ — within the deployment target, so no floor change. No new framework imports beyond `SwiftUI`.
- **Concurrency regime unchanged (D-013/D-040):** every new token type (`Theme`, `ThemeMode`, `BoardMetrics`, `Curve`, `Motion`, `ThemeEnvironmentKey`) is explicitly `nonisolated` (and `Sendable` where it holds state), and the `Color(hex:)` extension init is `nonisolated`, so the `EnvironmentKey`/`Color` conformances satisfy SwiftUI's `nonisolated` requirements under the app target's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` — the same rule D-040 set. App target still sets default-MainActor; the test target does not. **No engine/model/test file was touched.**
- **Toolchain still CLT-only (confirmed):** `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`; `xcodebuild`/`simctl` absent. macOS SDK used for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk` (the SDK's `SwiftUI.framework` is present, so the view code type-checks).
- **Verification performed in this environment (CLT only):**
  - The token layer (`Theme.swift` + `Motion.swift`) type-checks clean under the app regime (`swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -sdk <MacOSX.sdk>`) → **exit 0** — this validates the `Color(hex:)` isolation, the `EnvironmentKey` conformance, and the curve/spring signatures.
  - The **real** model sources type-check clean under the app regime (incl. `@Observable` expansion) → exit 0 — confirms the engine is still well-formed and untouched.
  - The full app (models + theme + the real `BoardView.swift`/`ContentView.swift`/`ECHOApp.swift`) type-checks clean under the app regime with the **CLT-only macro substitution** the prior phases established (`@State` swapped for a property wrapper with a `nonmutating set`; `#Preview` stripped) → **exit 0, no warnings** — this validates the new SwiftUI API usage end-to-end: `keyframeAnimator` with two `KeyframeTrack`s over a `Squash` value, the `.transaction { … }` enemy-retiming, the `accentGlow` `View` extension, `scaleEffect(x:y:)`, and `commitMove`'s read-only prediction logic. The only constructs not exercised here are the `@State`/`#Preview` macros themselves (CLT lacks their plugins); they are standard and expand in real Xcode.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–1.08 + hotfix):** the iOS Simulator build, the on-device ⌘R run (the real look + 60fps glide), the **⌘U** XCTest run (the green baseline + post-change re-run the brief asks for), a real screenshot, and the runtime `Levels/` bundling. **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers** — this environment has no Xcode, so they cannot be captured here. Capture on Lazar's Mac with `xcodebuild -version` **and** `xcodebuild -version -sdk iphoneos ProductBuildVersion` (or Xcode → About Xcode), and pin them in a follow-up entry here.


---

## 2026-06-27 — Phase 2.03 fold choreography & death dissolve (Code track)

The two signature events were animated: a new SwiftUI **Canvas** effect overlay (`ECHO/Views/BoardEffects.swift`), token additions in `ECHO/Theme/Motion.swift` (`Motion.Span` raw phase durations + `Ease` scalar cubic-Bézier easings), and the fold/death wiring in `ECHO/Views/BoardView.swift`. **No dependency, tool, or external-package change this phase** — still Swift 6.4, **zero** Swift Package dependencies, **no SpriteKit** (Canvas held the bounded particle budget, so the conditional SpriteKit fallback was not needed), deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new file rides the existing `ECHO` synchronized root group (no target-membership edit). The local-only `DEVELOPMENT_TEAM` line Xcode writes on build is left unstaged (public repo stays team-less).

- **System frameworks only (unchanged set):** SwiftUI. New API surface exercised this phase: `TimelineView(.animation)`, `Canvas` (with an `inout GraphicsContext` renderer), `GraphicsContext.Shading.radialGradient`, `Path(roundedRect:cornerRadius:style:)`, `.task(id:)` + `Task.sleep(for:)`, and `.onChange(of:_:)`. All are iOS 17.0+ — within the deployment target, so **no floor change**. No new framework imports beyond `SwiftUI` (the `BoardEffects` file imports only `SwiftUI`).
- **Concurrency regime unchanged (D-013/D-040):** the two new value types (`FoldEffect`, `DeathEffect`) are `nonisolated` + `Sendable`; `Ease` and `Motion.Span` are `nonisolated`; `BoardEffectsOverlay` is an ordinary `@MainActor` `View` and its `Canvas` renderer is a non-`Sendable`, non-`nonisolated` escaping closure (an ordinary parameter, **not** a `nonisolated` protocol requirement like `Shape.path(in:)`), so it is inferred main-actor-isolated and composes cleanly under `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` — **no `Shape` conformance was introduced**, so D-040 does not apply here. App target still sets default-MainActor; the test target does not. **No engine/model/level/test file was touched** (`git diff` empty over `ECHO/Models/*`, `Levels/*`, `ECHOTests/*`).
- **Toolchain still CLT-only (confirmed):** `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`; `xcodebuild`/`simctl` absent. macOS SDK used for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk`.
- **Verification performed in this environment (CLT only):**
  - Full app (models + theme + the real `BoardView`/`ContentView`/`ECHOApp` + the new `BoardEffects.swift`) type-checks clean under the app regime with `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -sdk <MacOSX.sdk>` and the established CLT macro substitution (`@State` → a `nonmutating`-set property-wrapper shim; `#Preview` stripped) → **exit 0, 0 warnings, 0 errors**. Validates `TimelineView`/`Canvas`/`GraphicsContext`/`radialGradient`, `Path(roundedRect:cornerRadius:style:)`, `.task(id:)` + `Task.sleep(for:)` under MainActor, `.onChange(of:)`, the `Motion.Span`/`Ease` additions, and the `commitMove`/`triggerFold`/`triggerDeath`/`finishDeath` flow.
  - Token layer alone (`Theme.swift` + `Motion.swift`) → exit 0; the engine/model sources alone under the app regime **and** the test regime → exit 0 each (engine still well-formed and untouched).
- **Still NOT verified here (no full Xcode — same caveat as 1.01–2.02):** the iOS Simulator build, the on-device ⌘R run (the fold + death effects + 60fps), the **⌘U** XCTest run (green baseline + post-change re-run), a real screenshot, and the runtime `Levels/` bundling. **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers** — uncapturable without Xcode; capture on Lazar's Mac with `xcodebuild -version` and pin in a follow-up entry here.

---

## 2026-06-27 — Phase 2.04 generative move-audio (Code track)

The first audio pass was added: a new `ECHO/Audio/AudioManager.swift` (one `AVAudioEngine`, a pool of `AVAudioPlayerNode`s, and a procedurally-synthesized sound bank), plus presentation-only trigger wiring in `ECHO/Views/BoardView.swift` and `ECHO/App/ContentView.swift`. **No external-package change — still zero Swift Package dependencies.** Deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new file rides the existing `ECHO` file-system-synchronized root group (auto-included, no target-membership edit). The local-only `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left unstaged (public repo stays team-less).

- **`AVAudioEngine` now in use (system framework — still no third-party package):** the audio file imports `Foundation` + `AVFoundation`. New API surface: `AVAudioEngine`, `AVAudioPlayerNode`, `AVAudioPCMBuffer`/`AVAudioFormat` (44.1 kHz mono float buffers synthesized at startup, **no bundled audio assets**), `AVAudioTime(hostTime:)` host-clock scheduling for sample-aligned chords, and `AVAudioSession` (`.ambient` + `.mixWithOthers`, **iOS-only — wrapped in `#if os(iOS)`** so the rest of the file type-checks against the macOS SDK). All are iOS 13/17-era APIs, well within the iOS 17.0 floor — **no deployment-floor change**. Per D-045, deliberately **no `AVAudioSourceNode`** real-time render block (keeps synthesis off the audio thread and the Swift-6 isolation story clean).
- **Concurrency regime unchanged (D-013):** `AudioManager` is a `@MainActor final class` that owns the engine and all (non-`Sendable`) nodes/buffers and touches them only from MainActor; `scheduleBuffer`'s completion handler is passed `nil` (no `@Sendable` capture of main-actor state). It is constructed in a `ContentView` `@State` and `start()`-ed from a `.task` at launch (idempotent; a no-op in SwiftUI previews via the `XCODE_RUNNING_FOR_PREVIEWS` guard). App target still sets default-MainActor; the test target does not. **No engine/model/level/test file was touched** (`git diff` empty over `ECHO/Models/*`, `Levels/*`, `ECHOTests/*`).
- **Toolchain still CLT-only (confirmed this phase):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode, but active developer directory '/Library/Developer/CommandLineTools' is a command line tools instance`; `xctrace`/`simctl` absent. `swiftc` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`. macOS SDK for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk`. **So the Phase-1.02 standing item — the exact Xcode 27 build number + iOS SDK build number — still cannot be captured here** (it requires full Xcode). It remains owed; capture on Lazar's Mac with `xcodebuild -version` (+ `xcodebuild -version -sdk iphoneos ProductBuildVersion`).
- **Verification performed in this environment (CLT only):**
  - Full app (models + theme + the real `BoardView`/`ContentView`/`ECHOApp` + `BoardEffects.swift` **+ the new `AudioManager.swift`**) type-checks clean under the app regime with `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -sdk <MacOSX.sdk>` and the established CLT macro substitution (`@State` → a `nonmutating`-set property-wrapper shim; `#Preview` stripped) → **exit 0, 0 errors**. Validates the AVFoundation API usage (engine/player-node graph, `AVAudioPCMBuffer` building via `floatChannelData`, `AVAudioTime.hostTime(forSeconds:)`/`AVAudioTime(hostTime:)`, `scheduleBuffer(_:at:options:completionHandler:)`), the synthesis math, and the `playStep`/`playFold`/`playDeath`/`playSolve` triggers + the moved-set derivation in `commitMove`.
  - The macOS-27 SDK emits **3 deprecation warnings** (`AVAudioPlayerNode.play()`, `AVAudioEngine.connect(_:to:format:)` — "deprecated in macOS 27.0"). These are **host-SDK artifacts, not real for this app**: re-running the type-check with `-target arm64-apple-macosx14.0` (a deployment target below the 27.0 deprecation, mirroring the iOS 17.0 < 27.0 situation) → **exit 0, ZERO warnings**. On the iOS 17.0 target these are the current, correct APIs (their iOS-27 async replacements don't exist below iOS 27, so adopting them would break the floor) — so the real iOS build is warning-free.
  - `AVAudioSession` (iOS-only) is behind `#if os(iOS)`, so its two lines (`setCategory(.ambient, options:[.mixWithOthers])`, `setActive(true)`) are **not** exercised by the macOS type-check — standard, low-risk API, to be confirmed on device.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–2.03):** the iOS Simulator/device build, the on-device ⌘R run (hearing the ticks/chords/fold/death/solve, the silent-switch behaviour, no clipping on a busy turn), the **⌘U** XCTest run (green baseline + post-change re-run — engine/models/tests untouched, so expected green), and the runtime `Levels/` bundling. **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers**.

---

## 2026-06-28 — Phase 2.05 Haptics (Code track)

Touch feedback was added: a new `ECHO/Haptics/HapticsManager.swift` (a `@MainActor final class` owning the three system `UIFeedbackGenerator`s), plus presentation-only trigger wiring in `ECHO/Views/BoardView.swift` and `ECHO/App/ContentView.swift`, and one additive read-only model signal (`MoveOutcome` + `lastMoveOutcome`) in `ECHO/Models/GameState.swift` with six new tests in `ECHOTests/ECHOTests.swift`. **No external-package change — still ZERO Swift Package dependencies.** Deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new file rides the existing `ECHO` file-system-synchronized root group (auto-included, like `AudioManager.swift`; both have zero `.pbxproj` references). The local-only `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left unstaged (public repo stays team-less).

- **`UIKit` now in use (system framework — still no third-party package):** `HapticsManager.swift` imports `UIKit` for `UISelectionFeedbackGenerator` / `UIImpactFeedbackGenerator(style: .medium)` / `UINotificationFeedbackGenerator` (`.error` / `.success`). These are iOS-13/17-era APIs, well within the iOS 17.0 floor — **no deployment-floor change**. The entire UIKit surface (import + the three stored generators + every call) is behind `#if canImport(UIKit)`, with `isEnabled` and the four method signatures **outside** the guard, so on the macOS dev SDK (where UIKit is absent) the type compiles to a real public **no-op skeleton** — the same approach as `AudioManager`'s iOS-only `AVAudioSession` `#if os(iOS)` guard. **No `CHHapticEngine`; `CoreHaptics` is not imported** (`UIFeedbackGenerator` self-degrades to a silent no-op on non-haptic hardware/Simulator, so no capability query is needed). Custom audio-synced `CHHapticPattern`s deferred (D-049).
- **Concurrency regime unchanged (D-013):** `HapticsManager` is a `@MainActor final class` owning the non-`Sendable` generators and touching them only from MainActor; it is constructed in a `ContentView` `@State` and pre-warmed from a `.task` at launch (idempotent; no-op where unsupported). The additive `MoveOutcome` is a top-level `nonisolated enum: Equatable, Sendable` (matching the existing `Direction`/`GridCoordinate` value-type pattern), and `lastMoveOutcome` is a MainActor-isolated `private(set) var` on `GameState`, read by no view body (so the `@Observable` property adds no view-invalidation cost). App target still sets default-MainActor; the test target does not. **No engine rule, level/JSON, or existing test was changed** — the only model edit is the additive signal.
- **Toolchain still CLT-only (confirmed this phase):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode … is a command line tools instance`. `swiftc` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`. macOS SDK for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk` (SwiftUI present). **So the Phase-1.02 standing item — the exact Xcode 27 build number + iOS SDK build number — still cannot be captured here** (it requires full Xcode); capture on Lazar's Mac with `xcodebuild -version` (+ `xcodebuild -version -sdk iphoneos ProductBuildVersion`).
- **Verification performed in this environment (CLT only):**
  - **App regime:** full app (models + theme + the real `BoardView`/`ContentView`/`ECHOApp` + `BoardEffects.swift` + `AudioManager.swift` **+ the new `HapticsManager.swift`**) type-checks clean under `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -sdk <MacOSX.sdk>` with the established CLT macro substitution (`@State`/`@Environment` → property-wrapper shims; `#Preview` stripped) → **exit 0**. The only warnings are the **3 pre-existing** `AVAudioPlayerNode.play()` / `connect(_:to:format:)` macOS-27 deprecations in `AudioManager.swift` (host-SDK artifacts — re-checking with `-target arm64-apple-macosx14.0` → **0 warnings**). `HapticsManager.swift` and every 2.05 edit contribute **0 diagnostics** (verified in isolation too).
  - **Test regime (suite actually run, not just type-checked):** a standalone `swiftc` harness built the `ECHO` model as an `-enable-testing` module (no `-default-isolation MainActor`, the test target's regime) + an `XCTest` shim module, compiled the **real** `ECHOTests.swift` + `RoomSolvabilityTests.swift` against them, and **ran the full suite against the real engine** → **67 methods / 734 assertions, 0 failures** (prior 61 / 712; **+6 methods / +22 assertions** for the move-outcome signal). The ⌘U-equivalent for the deterministic logic; confirms the additive change regressed nothing. (The harness is a throwaway dev tool, not committed.)
  - **Adversarial 4-lens agent review** of the diff (UIKit/degradation+concurrency / spec+DoD / model-additivity / wiring+sync): zero real defects; one doc nit (comments overstated death/win tap–sound co-arrival) **fixed**.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–2.04):** the iOS Simulator/device build, the on-device **⌘R** to **feel** the four taps (Core Haptics needs a physical iPhone; the Simulator emits none), the **⌘U** XCTest run (green baseline + post-change re-run — expected green), a real screenshot, and the runtime `Levels/` bundling. **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers**.

---

## 2026-06-28 — Phase 2.06 Settings, persistence, the echo-trail aid & the guidance microcopy (Code track)

The four design toggles got a real, persisted home and two new presentation layers landed. New files: `ECHO/Settings/SettingsStore.swift` + `ECHO/Settings/SettingsView.swift`, `ECHO/Guidance/GuidanceController.swift` + `ECHO/Guidance/GuidanceOverlay.swift`, and `ECHOTests/SettingsGuidanceTrailTests.swift`. Edited (all additive / presentation-only): `ECHO/App/ContentView.swift`, `ECHO/Views/BoardView.swift`, `ECHO/Models/Echo.swift` (additive pure `upcomingCells`), `ECHO/Theme/Theme.swift` (additive `BoardMetrics` trail tokens) and `ECHO/Theme/Motion.swift` (additive `trailFadeOut` + guidance/trail `Span`s). **No external-package change — still ZERO Swift Package dependencies.** Deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new `ECHO/Settings/` and `ECHO/Guidance/` subfolders ride the existing `ECHO` file-system-synchronized root group (auto-included, like `ECHO/Audio/`/`ECHO/Haptics/`; zero `.pbxproj` references), and the new test file rides the `ECHOTests` group. The local-only `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left unstaged (public repo stays team-less).

- **UserDefaults keys introduced (no new framework):** `settings.invertEnabled` / `settings.soundEnabled` / `settings.hapticsEnabled` / `settings.echoTrailEnabled` (the four prefs), and `guidance.seen.<hint>` for each one-time hint's seen-once flag. `UserDefaults` is Foundation (already imported); no new dependency. Both new stores take an injectable `UserDefaults` (default `.standard`) so the tests use isolated suites.
- **Additive token constants (no value changed):** `BoardMetrics.trailDotSize` 3 / `trailDotSpacing` 8 / `trailDotOpacity` 0.40; `Motion.trailFadeOut` (150 ms `curve.easeIn`); `Motion.Span.trailFade` 0.150 / `guidanceIn` 0.200 / `guidanceOut` 0.350 / `guidanceLingerHint` 2.2 / `guidanceLingerFeedback` 1.6. All new symbols; no existing token edited.
- **Concurrency regime unchanged (D-013):** `SettingsStore` and `GuidanceController` are `@MainActor @Observable final class`es (Foundation + Observation only — no SwiftUI), each owning a non-observed (`@ObservationIgnored`) injected `UserDefaults`; constructed in `ContentView` `@State`s like the managers. `Echo.upcomingCells` is `nonisolated` (it is on the existing `nonisolated` value type). `EchoTrailLayer`/`TrailDot` are private view/value types in `BoardView`. App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not. **`GameState` and every other engine/model file is byte-for-byte unchanged except the additive `Echo.upcomingCells`; no level JSON, no existing test changed.**
- **Toolchain still CLT-only (confirmed this phase):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode … is a command line tools instance`. `swiftc` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`. macOS SDK for type-checks: `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk`. **So the Phase-1.02 standing item — the exact Xcode 27 build number + iOS SDK build number — still cannot be captured here** (it requires full Xcode); capture on Lazar's Mac with `xcodebuild -version` (+ `xcodebuild -version -sdk iphoneos ProductBuildVersion`).
- **Verification performed in this environment (CLT only):**
  - **App regime:** the full app (models + theme + the real `BoardView`/`ContentView`/`ECHOApp` + `BoardEffects.swift` + `AudioManager.swift` + `HapticsManager.swift` **+ the new `SettingsStore`/`SettingsView`/`GuidanceController`/`GuidanceOverlay`**) type-checks clean under `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -sdk <MacOSX.sdk>` with the established CLT macro substitution (`@State`/`@Environment` → property-wrapper shims; `#Preview` stripped) → **exit 0**. The only warnings are the **3 pre-existing** `AVAudioPlayerNode.play()` / `connect(_:to:format:)` macOS-27 deprecations in `AudioManager.swift` (host-SDK artifacts — re-checking with `-target arm64-apple-macosx14.0` → **exit 0, 0 warnings**). Every 2.06 file contributes **0 diagnostics**.
  - **Test regime (suite actually run, not just type-checked):** a standalone `swiftc` harness built the `ECHO` model as an `-enable-testing` module (no `-default-isolation MainActor`) — now including `SettingsStore.swift` + `GuidanceController.swift` — + an `XCTest` shim, compiled the **real** `ECHOTests.swift` + `RoomSolvabilityTests.swift` + `SettingsGuidanceTrailTests.swift` against them, and **ran the full suite against the real engine** → **80 methods / 781 assertions, 0 failures** (prior 67 / 734; **+13 methods / +47 assertions** for settings persistence/defaults, the guidance mapping/verbatim-strings/seen-once gate/message triggers, and the echo upcoming-path list). (Harness is a throwaway dev tool, not committed.)
  - **Adversarial 5-lens agent review** of the diff (spec-conformance / presentation-only invariants / concurrency-isolation / wiring-persistence / trail-guidance render-math), each finding then adversarially verified.
- **Still NOT verified here (no full Xcode — same caveat as 1.01–2.05):** the iOS Simulator/device build, the on-device **⌘R** (the Settings sheet + the live effect of each toggle; the echo-trail dots/reveal/fade in both palettes; the guidance microcopy fades + placement; both palettes), a real screenshot, the **⌘U** XCTest run, and the runtime `Levels/` bundling. The audio is still unheard and the haptics unfelt (carried from 2.04/2.05). **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers**.

> Phases **2.07** and **3.01** changed nothing in the stack/config (no package, target, or tool change; presentation-only / pure-content) and did not append here — the live tip before 3.02 is the **20-room campaign**, suite **94 methods / 1419 assertions** (per `current-state.md` / the 3.01 completion report), built on the 2.06 stack recorded above.

## 2026-06-28 — Phase 3.02 Echo Run (arcade survival mode) (Code track)

A second game mode landed as a **separate, additive engine** (D-057/D-058) with no stack change. **New files:** `ECHO/Models/EchoRunState.swift` (the arcade engine + the `nonisolated`/`Sendable` value types `RunEcho`/`RunOutcome`/`PendingDeath`), `ECHO/Views/EchoRunView.swift` (the arcade board screen), `ECHOTests/EchoRunTests.swift`. **Edited (additive):** `ECHO/Settings/SettingsStore.swift` (the persisted `Int` `echoRunHighScore` + `recordEchoRunScore`) and `ECHO/App/ContentView.swift` (the interim Echo Run entry + `body` mode switch). **No external-package change — still ZERO Swift Package dependencies.** Deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new sources ride the existing `ECHO` and `ECHOTests` file-system-synchronized root groups (auto-included). The local-only `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left unstaged.

- **New `UserDefaults` key (no new framework):** `echoRun.highScore` (`Int`, default 0 — the Plan §6 second persisted value), through the existing `SettingsStore` wrapper (Foundation `UserDefaults`, already imported), not a second persistence system (D-058). Injectable `UserDefaults` as before, so the round-trip is unit-tested in an isolated suite.
- **Concurrency regime unchanged (D-013):** `EchoRunState` is a `@MainActor @Observable final class`; the new value types (`RunEcho`/`RunOutcome`/`PendingDeath`) are top-level `nonisolated` + `Sendable`, matching `GridCoordinate`/`Direction`/`MoveOutcome`. App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not. **`GameState`, `Echo`, `Hazard`, `Level`, `LevelLoader`, `GridCoordinate`, `Direction`, every `Levels/*.json`, and every existing test are byte-for-byte unchanged** — Echo Run only adds.
- **Toolchain still CLT-only (confirmed this phase):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode … is a command line tools instance`. `swiftc` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`. **The Phase-1.02 standing item — the exact Xcode 27 build number + iOS SDK build number — still cannot be captured here.**
- **Verification performed in this environment (CLT only):**
  - **App regime:** the full app (models + theme + the real `BoardView`/`ContentView`/`ECHOApp` + `BoardEffects`/`AudioManager`/`HapticsManager`/settings/guidance **+ the new `EchoRunView`** and `EchoRunState`) type-checks clean under `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -target arm64-apple-macosx14.0` with the established CLT macro substitution (`@State`/`@Environment` → property-wrapper shims; `#Preview` stripped) → **exit 0, 0 warnings** (the `withAnimation` unused-result warning was fixed to `_ = state.move(…)`, matching `BoardView`).
  - **Test regime (suite actually run, not just type-checked):** the standalone `swiftc` + `XCTest`-shim harness built the `ECHO` model as an `-enable-testing` module (Models + `SettingsStore` + `GuidanceController`), compiled the **real** `ECHOTests.swift` + `RoomSolvabilityTests.swift` + `SettingsGuidanceTrailTests.swift` + **`EchoRunTests.swift`** against it, and **ran the full suite against the real engines** → **109 methods / 1596 assertions, 0 failures** (3.01 tip 94 / 1419; **+15 methods / +177 assertions**, all the new arcade coverage). (Harness is a throwaway dev tool, not committed.)
- **Still NOT verified here (no full Xcode — carried):** the iOS Simulator/device build, the on-device **⌘R** (Echo Run's look/score/death dissolve/game-over/Retry, both palettes; the audio chord of stacked shadows still unheard, the haptics unfelt), a real screenshot, the **⌘U** XCTest run, and the runtime `Levels/` bundling (Echo Run loads no level JSON, but the app build on device is still unverified). **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers**.

> Phase **3.03** (the navigation shell) changed nothing in the stack/config (no package/target/tool change; new sources ride the existing synchronized groups) and did not append here — its details live in `current-state.md` and the 3.03 completion report. The live tip before 3.04 is suite **128 methods / 1685 assertions**.

## 2026-06-28 — Phase 3.04 Final feel + performance: device-gated groundwork (Code track)

The two carried transition artifacts were fixed and a developer-only performance overlay landed, with no stack change. **New files:** `ECHO/Diagnostics/FrameStats.swift` (pure `FrameWindow`/`FrameStats` math), `ECHO/Views/PerformanceOverlay.swift` (the `#if DEBUG` overlay + `CADisplayLink` monitor + `.debugPerformanceOverlay()` modifier), `ECHOTests/FrameStatsTests.swift`, `docs/feel-audit.md`. **Edited (presentation-only):** `ECHO/Views/RoomView.swift` (the win-overlay pre-delay + the opaque-paper advance cover, D-064; mounts the overlay) and `ECHO/Views/EchoRunView.swift` (mounts the overlay). **No external-package change — still ZERO Swift Package dependencies.** Deployment target `IPHONEOS_DEPLOYMENT_TARGET = 17.0`, bundle id `com.dinovlazar.echo`, display name `Echo`, all unchanged. **No `.pbxproj` structural edit** — the new `ECHO/Diagnostics/` subfolder rides the existing `ECHO` file-system-synchronized root group (auto-included, like `ECHO/Audio/`/`ECHO/Settings/`; zero `.pbxproj` references), and the new test file rides the `ECHOTests` group. The local-only `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left unstaged (public repo stays team-less).

- **No new framework / UserDefaults key / token value.** The overlay uses `CADisplayLink` (`QuartzCore`, via `import UIKit` on iOS — no package); the two new constants are presentation timings in `RoomView` (`winRevealDelay` ≈ 0.45 s, `advanceCoverFade` = `Motion.guidanceIn`) — both named, commented "tune on device" first guesses (D-064), not a change to any locked feel value.
- **Compile-time release gate (D-063):** the overlay and its monitor/readout are entirely inside `#if DEBUG`; `View.debugPerformanceOverlay(_:)` resolves to a **no-op in release** (verified: the type-check's release path compiles the `#else self` branch). The `CADisplayLink` is additionally behind `#if canImport(UIKit)` (off-iOS a no-op skeleton, like `HapticsManager`). The pure `FrameStats`/`FrameWindow` math is **not** gated (harmless pure code, never instantiated in release) so it stays headlessly testable without a `-D DEBUG` flag.
- **Concurrency regime unchanged (D-013):** `FrameRateMonitor` is a `@MainActor @Observable final class`; `FrameWindow`/`FrameStats` are top-level `nonisolated`/`Sendable` value types (Foundation only), matching `GridCoordinate`/`Direction`/`Campaign` (D-040). App target still sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; the test target does not. **`GameState`, `Echo`, `Hazard`, `Level`, `LevelLoader`, `GridCoordinate`, `Direction`, `EchoRunState`, every `Levels/*.json`, and every prior test are byte-for-byte unchanged** — 3.04 only adds (plus the two presentation edits in `RoomView`/`EchoRunView`).
- **Toolchain still CLT-only (confirmed this phase):** `xcodebuild -version` → `xcode-select: error: tool 'xcodebuild' requires Xcode … is a command line tools instance`. `swiftc` → `Apple Swift version 6.4 (swiftlang-6.4.0.23.5 clang-2100.3.23.3)`, target `arm64-apple-macosx27.0.0`. **The Phase-1.02 standing item — the exact Xcode 27 build number + iOS SDK build number — still cannot be captured here.**
- **Verification performed in this environment (CLT only):**
  - **App regime (both build configurations):** the full app type-checks clean under `swiftc -typecheck -parse-as-library -swift-version 6 -default-isolation MainActor -strict-concurrency=complete -target arm64-apple-macosx14.0` with the established CLT macro substitution (`@State`/`@Environment` → property-wrapper shims; `#Preview` stripped) → **exit 0, 0 warnings** both **without** `-D DEBUG` (the release path, overlay compiled out) **and with** `-D DEBUG` (the overlay's modifier/readout/monitor skeleton compiled). The iOS-only `CADisplayLink` body (behind `#if canImport(UIKit)`) is device-checkable-only, exactly like `AudioManager`'s `AVAudioSession` and `HapticsManager`'s `UIKit` surface.
  - **Test regime (suite actually run, not just type-checked):** the standalone `swiftc` + `XCTest`-shim harness built the `ECHO` model as an `-enable-testing` module — now also compiling **`ECHO/Diagnostics/FrameStats.swift`** (the model-module input list adds that one file; `Settings`/`Guidance` are still picked file-by-file, **not** dir-globbed, since each holds a SwiftUI view too) — compiled the **real** `ECHOTests.swift` + `RoomSolvabilityTests.swift` + `SettingsGuidanceTrailTests.swift` + `EchoRunTests.swift` + `NavigationTests.swift` + **`FrameStatsTests.swift`**, and **ran the full suite against the real engines** → **140 methods / 1724 assertions, 0 failures** (3.03 tip 128 / 1685; **+12 methods / +39 assertions**, the new frame-math coverage). (Harness is a throwaway dev tool, not committed.)
  - **Adversarial multi-agent review** of the diff + new files (correctness/concurrency, scope/spec-compliance, design-discipline, DoD-completeness), each finding then adversarially verified.
- **Still NOT verified here (no full Xcode — carried, and this is the explicit point of the 3.04 split, D-062):** the performance overlay can never render real frames under CLT, so **60fps / no-jank is NOT judged this phase** — only the overlay's pure math is tested; the two transition fixes are not seen; audio unheard, haptics unfelt; the iOS Simulator/device build, on-device **⌘R**, a real screenshot, the **⌘U** run, and the runtime `Levels/` bundling all remain owed. **Pinning still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers**.
