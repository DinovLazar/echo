# current-state.md

> A snapshot of the repo **right now** — a mirror, not a changelog. Code **overwrites** the stale parts at the end of every phase; it does not append. If this and `ECHO-Plan.md` ever disagree, **this file wins** (it reflects reality). The running history lives in completion reports and `00_stack-and-config.md`.

**Last updated:** 2026-06-28 — **Phase 3.01** (The full campaign — ten new hand-authored rooms `room-11`…`room-20`, registered in order so play runs straight through `room-01 → room-20` and ends at the finale). On top of the merged Part 2 presentation work through Phase 2.07 (in-room HUD & solid walls), 2.06 (settings/persistence/echo-trail/guidance), 2.05 (haptics), 2.04 (audio), 2.03 (fold/death effects), 2.02 (board look + motion).

---

## Summary (plain language)
- **Phase 3.01 (most recent change) — pure content, zero engine risk.** ECHO now has a **20-room campaign with a real difficulty curve and a clear ending**. Ten new rooms (`Levels/room-11.json`…`room-20.json`) were hand-authored in the **locked v1 level format** (D-024) — no engine, model, or JSON-format change — and registered, in order, in the campaign id list in `ContentView` so `Next` runs `room-01 → … → room-20`, with `room-20` ("Coda") the last room.
  - **The curve (rooms 11–20):** Band A *Recombine & widen* — 11 "Crossroads" (two held gates in series), 12 "Both at Once" (the first **AND-door**: one door whose `heldBy` lists two switches, so two echoes must hold it at once), 13 "Wide Gate" (the first **multi-cell door**: one switch opens a two-tile gate, a patrol makes the low straight run fatal so you cross on the wide gate's other tile). Band B *Pressure & multiplicity* — 14 "Twin Patrols" (**two** simultaneous patrols sweep a corridor), 15 "The Long Hold" (a long-period patrol; one echo settles early and patiently holds through the whole circuit), 16 "Three" (**budget 3**: three held gates, four live timelines), 17 "Threadneedle" (AND-door + two patrols). Band C *Choreography & finale* — 18 "Clockwork" (AND-door + single gate + a column-clocking patrol, budget 3), 19 "Lattice" (the hardest: a **multi-cell AND-door** — a two-tile gate held by two switches — + two out-of-phase patrols, budget 3), 20 "Coda" (the finale: a gate·patrol·gate·patrol clockwork that ends the campaign cleanly, budget 3).
  - **Every previously-unused capability of the locked format is now exercised in real play:** AND-doors `heldBy.count ≥ 2` (rooms 12, 17, 18, 19, 20), multi-cell doors `cells.count ≥ 2` (rooms 13, 19), rooms with **two** simultaneous hazards (14, 17, 19, 20), and **budget-3** rooms (16, 18, 19, 20).
  - **Tests:** `ECHOTests/RoomSolvabilityTests.swift` gained one solvability test per new room (`assertSolves` with a verified reference solution that wins within the room's `echoBudget`), four negative "naive line fails" tests (room-12's one-echo AND-door stays shut; rooms 13/14/17's naive straight runs die to a patrol), and the hazard-trace test was extended via a new `traceAt(id, index, …)` helper to cover **every** new hazard (both hazards in the two-patrol rooms). **Verified run** (the ⌘U-equivalent harness): **94 test methods / 1419 assertions, 0 failures** (was 80 / 781 at the 2.07 tip — exactly +14 methods, all new).
  - **Decision:** **D-056** records the block-vs-bite resolution (echoes bite everywhere — the shipped land-on/cross-paths death — with no first-room "refused move" exception; the unwired fifth guidance string + the §8.1 deny-shake are formally cut). It **resolves the open question D-052 left**, and D-052's Status line now points to it. (The Phase 3.01 brief proposed this as "D-053", but D-053/054/055 were already taken by Phase 2.07; per the repo's next-free-id rule it lands at D-056 — see report §3.)
  - **One flagged budget change:** room 13 "Wide Gate" ships at **`echoBudget` 1**, not the intent table's 2 (a −1 adjustment, within the brief's ±1 allowance). The room has a single switch, so a second echo would be useless; budget 1 is the lean cap and the cheese-check confirms one echo is needed and sufficient (report §4).
- **Branch / merge context:** `main` already contains all of Part 2 (2.02–2.07 merged; PR [echo#6] and [echo#7]). Phase 3.01 branched cleanly from that `main` as `phase-3.01-campaign-rooms` and touches only level JSON, the one-line campaign id list in `ContentView`, the test suite, and the decision/state docs — it was independent of the Part 2 presentation work and did not conflict.
- **Engine + tests untouched as rules:** `GameState`, `Echo`, `Hazard`, `Level`, `LevelLoader`, `GridCoordinate`, `Direction` are **byte-for-byte unchanged** (`git status` shows no `ECHO/Models/` edits). No turn/collision/win/replay/budget rule, JSON format, or **existing** room/test was changed — this phase only **adds**. `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` untouched (D-013).
- **Verified here (Command Line Tools only — no Xcode):**
  - **Test suite actually run (not just type-checked):** the standalone `swiftc` + `XCTest`-shim harness built the `ECHO` model as an `-enable-testing` module, compiled the **real** `ECHOTests.swift` + `RoomSolvabilityTests.swift` + `SettingsGuidanceTrailTests.swift`, and **ran the whole suite against the real engine**: **94 methods / 1419 assertions, 0 failures.** This exercises, end to end and for the first time in real play, every new-capability combination (AND-doors, multi-cell doors, ≥2 simultaneous hazards, budget-3) — the JSON decodes through the real `Level` Decodable, all ten reference solutions win within budget through the real `GameState`, every hazard trace matches the real `Hazard.position(at:)`, and the negative lines fail exactly as designed.
  - **Authoring rigour (off-repo design harness, not committed):** every room was designed against an exact Python mirror of the engine (validated to reproduce all ten shipped rooms' reference solutions and all five documented hazard traces). Each new room was checked for: solvability at its stated budget; the **minimum** number of switch-holding echoes actually required (so the budget is the lean budget, not a loose one — no room falls to a cheaper line); replay discipline (no echo and no hazard is ever on a wall or a closed-door tile across the whole solution, D-020/D-036); and the named negative lines.
- **NOT done here (needs Lazar's physical iPhone + full Xcode — this Mac has only Command Line Tools):** the on-device build (**⌘R**) and the **⌘U** test run in Xcode — never performed on the real game (only the harness equivalent); the **runtime confirmation that `Levels/` JSON bundles into the app and loads on device** (D-025) — still unconfirmed, and the ten new rooms ride the same mechanism so they share this risk; the **feel** of Part 2 (audio heard, haptics felt, motion/effects/look seen) — judged only in code (2.02–2.07); the exact **Xcode 27 / iOS SDK build numbers** (owed in `00_stack-and-config.md` since 1.02). These are carried forward honestly, un-checked.
- **Current phase:** 3.01 — **code complete to the limit this toolchain allows.** All ten rooms exist and decode, are registered in order ending at `room-20`, are solvable within budget with verified reference solutions, exercise every previously-unused format capability, and pass the full green suite. D-056 + D-052 status line filed; state docs synced. The on-device build/run/feel pass is owed on Lazar's iPhone.
- **Next:** the rest of Part 3 — the real Title / Main-Menu / Level-Select / win overlay, and on-device distribution (SideStore). Those navigation phases replace the interim `Next` debug room-cycle with real Level-Select over this 20-room campaign and add the `fold == nil && death == nil` input-lock gate (D-037).

## Detail
- **`Levels/room-01.json` … `room-10.json`** — the ten teaching rooms (D-033). **Unchanged this phase.** Real.
- **`Levels/room-11.json` … `room-20.json`** — **new (Phase 3.01).** The ten campaign rooms, locked v1 format (D-024). Bundled into the app target via the `Levels` synchronized root group (same mechanism as the teaching rooms; D-025 bundling unconfirmed on device). Real.
- **`ECHO/App/ContentView.swift`** — root view. **This phase:** the campaign `roomIDs` list is extended from `room-01…room-10` to `room-01…room-20` (one additive edit + its doc comment), so `Next` cycles the full campaign and ends at `room-20`. Nothing else changed; it still owns the `GameState`, index, `SettingsStore`, `GuidanceController`, `AudioManager`, `HapticsManager`, the top HUD + control bar (2.07), etc. The interim `Next` is still a debug room-cycle (real Level-Select is a later Part-3 phase, D-037). Real.
- **`ECHOTests/RoomSolvabilityTests.swift`** — the per-room solvability suite. **This phase:** +10 `testRoom{11..20}{Name}()` solvability tests (verified reference solutions via the existing `assertSolves`), +4 negative tests (room-12 AND-door one-echo-shut; rooms 13/14/17 naive-straight-dies), and `testHazardTracesMatchDocumentedPaths` extended with a `traceAt(id, index, …)` helper + one one-period trace per new hazard (both hazards for the two-patrol rooms). The existing room-01…10 tests are untouched. Real.
- **`ECHO/Models/GameState.swift` / `Echo.swift` / `Hazard.swift` / `Level.swift` / `GridCoordinate.swift` / `Direction.swift`** — the engine + pure value types + level schema/loader. **Byte-for-byte unchanged this phase.** Real.
- **Everything else (`ECHOApp`, `BoardView`, `BoardEffects`, `ControlButtonStyle`, `Theme`, `Motion`, `AudioManager`, `HapticsManager`, `SettingsStore`/`SettingsView`, `GuidanceController`/`GuidanceOverlay`, `Assets.xcassets`, `ECHOTests.swift`, `SettingsGuidanceTrailTests.swift`)** — **unchanged this phase.** Real.
- **`ECHO-Decisions.md`** — **this phase:** appended **D-056** (block-vs-bite resolved) and updated **D-052**'s Status line to point to it. The log now reads continuously from 2.06 (D-050/051/052) through 2.07 (D-053/054/055) to 3.01 (D-056).
- **`ECHO.xcodeproj`** — file-system synchronized groups; the new `Levels/room-11…20.json` ride the existing `Levels` synchronized root group and are picked up automatically (no `.pbxproj` structural edit). The `DEVELOPMENT_TEAM = X74CK53A6Q` line Xcode writes on build is left **unstaged** (repo rule; the only working-tree edit to `project.pbxproj`).

## Build & repo status
- **Project file:** `ECHO.xcodeproj` at repo root; SwiftUI App lifecycle; bundle id `com.dinovlazar.echo`; display name **Echo**; deployment target **iOS 17.0**; Swift language mode 6.0 (Swift 6.4 toolchain); app target sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (D-013); iPhone-only. **No external packages (still zero).**
- **Signing:** "Automatically manage signing" on; **no `DEVELOPMENT_TEAM` in the repo** — Xcode re-adds it to the working-tree `.pbxproj` on each build; leave it unstaged.
- **Verification performed here (Command Line Tools only; `swiftc` 6.4 present, no Xcode):** the `ECHO` model module builds clean as an `-enable-testing` module; the real test files compile and the suite **runs green — 94/94 methods, 1419/1419 assertions** — against the real engine via the `swiftc` + `XCTest`-shim harness. All ten new room JSON files lint as valid JSON and decode through the real `Level` Decodable.
- **NOT verified here (needs Lazar's physical iPhone + full Xcode):** the iOS Simulator/device build (**⌘R**), the in-Xcode **⌘U**, a real screenshot, and the runtime `Levels/` bundling (D-025 — the new rooms share this unconfirmed mechanism). **Still owed (carried from 1.02):** the exact **Xcode 27 / iOS SDK build numbers** — uncapturable here (no Xcode).
- **Remote:** `origin` = `https://github.com/DinovLazar/echo` (public). **Branch:** Phase 3.01 work is on **`phase-3.01-campaign-rooms`**, cut from `main` (which already contains 2.02–2.07). Opened as a PR into `main`.

## Known issues / carried forward (still owed, not done)
- **On-device build/run/test never performed:** the **⌘R** on the real iPhone and the in-Xcode **⌘U** have never run on the real game (only the headless harness equivalent). Owed on Lazar's device.
- **D-025 `Levels/` runtime bundling unconfirmed:** that the JSON actually bundles into the app and loads on device is still unverified; the ten new rooms ride the same mechanism, so they share this risk.
- **room 13 budget −1 (flagged, not silent):** "Wide Gate" ships at `echoBudget` 1 vs the intent table's 2 — the single-switch design makes a second echo useless and the cheese-check confirms 1 is needed-and-sufficient (report §4).
- **Part 2 feel still unjudged:** the audio (2.04) has not been heard, the haptics (2.05) not felt, and the motion/effects/look (2.02/2.03/2.06/2.07) seen only in code.
- **Toolchain pin owed since 1.02:** exact Xcode 27 / iOS SDK build numbers, uncapturable without full Xcode.
- **Interim navigation persists (D-017/D-037/D-054):** `Next` is still a debug room-cycle (now over 20 rooms), `Clear` is debug-only, and the controls mutate `state` directly (bypassing the `fold == nil && death == nil` input lock) — all replaced by the real Part-3 Level-Select/navigation phases.
- **Dead guidance string owed for removal (D-056, out of scope this phase):** D-056 cut the fifth guidance string from scope, but `ECHO/Guidance/GuidanceController.swift` still defines `GuidanceFeedback.blockedByGhost` and its header still says "Five strings" — now **dead code** (guidance is out of scope for 3.01). The next phase that touches guidance should delete the constant, change the header to "Four strings," and drop the "unwired fifth string" comments.
- **Carried authoring/repo rules unchanged:** D-020/D-036 (verbatim replay — discipline held: no echo/hazard ever on a wall or closed-door tile across any solution), D-012 (no `.gitkeep` inside `ECHO/`/`ECHOTests/`), `DEVELOPMENT_TEAM` kept out of the repo (unstaged).
