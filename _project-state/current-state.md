# current-state.md

> A snapshot of the repo **right now** — a mirror, not a changelog. Code **overwrites** the stale parts at the end of every phase; it does not append. If this and `ECHO-Plan.md` ever disagree, **this file wins** (it reflects reality). The running history lives in completion reports and `00_stack-and-config.md`.

**Last updated:** 2026-06-26 — end of **Phase 1.08 (The first teaching rooms)**

---

## Summary (plain language)
- **Works now:** The whole 1.03–1.07 engine (move; hidden shared turn counter; fold→grey echo replay in lockstep; touching an echo or a hazard dissolves present-you and restarts the run while folded echoes survive; per-room walls / switches+doors / patrolling lethal hazards derived per turn; win on reaching the exit alive; `echoBudget` cap; plus **step back** and **reset run**) — **now with ten handcrafted teaching rooms wired into a playable sequence (Phase 1.08).**
- **The ten rooms (`Levels/room-01.json` … `room-10.json`):** a guided ramp, one new idea at a time then combined (D-033) — 01 move, 02 walls, 03 first fold, 04 echo-as-lethal-obstacle, 05 two selves (two doors in series), 06 patrol, 07 hold-then-time, 08 two jobs (with a switch dependency), 09 contested, 10 capstone. Echo budgets `0,0,1,1,2,0,1,2,2,2`. Every fold-room is **unsolvable with its doors closed** (so folding is genuinely forced), and rooms 09–10 deliberately "bite" (D-035). No new engine mechanics were added.
- **Play sequence:** the throwaway debug bar's **Next** control now cycles `room-01 → … → room-10` (wrapping). The three throwaway proof rooms (`p1-06-a/b/c.json`) were **removed from the sequence and deleted** (no test referenced them). There is still **no Level Select, no saved progress, and no win/transition overlay** — those stay Part 3 (D-037); losing app state restarts at room 01.
- **Two rooms were adjusted from the brief to match the engine's real door timing.** The engine reads a door's open-state at the **pre-step** turn (`isClosedDoor(target)` runs in `move(_:)` *before* the turn ticks), so a switch-held door is passable only when the holding echo is already on the switch the turn the player is **adjacent** to it. Two of the brief's tight-timing rooms were calibrated to a post-step reading and were fixed:
  - **Room 05** — geometry kept exactly; the final-run **stall was lengthened** to `up,down,up,down` so door A is entered at the right (even-parity) turn (D-038).
  - **Room 09** — the brief's 5×6 layout is genuinely **unsolvable** under the pre-step rule (the live player has no legal first move after folding both echoes), so it was **regrown to 5×7 with a one-cell stall pocket above the start**, preserving budget 2, the lesson, and the overall shape (D-039).
- **Tests:** a new `@MainActor` suite `ECHOTests/RoomSolvabilityTests.swift` (D-034) loads each room and replays a **verified reference solution** through the **real** `GameState`, asserting every move lands, each fold banks one echo within budget, and the final run reaches the exit with `hasWon`. Rooms 04/06/07 also pin their "naive run must fail" negative, and a hazard-trace test checks each patrol's one-period trace (and wrap). These join the existing deterministic suite in `ECHOTests.swift` (unchanged).
- **No code rules changed:** `GameState`, `Echo`, `Hazard`, `Level`, `GridCoordinate`, `Direction`, and `BoardView` are **untouched** this phase. Only `ContentView`'s room-id list changed (proof rooms → the ten teaching rooms) plus comment refreshes. Visuals stay pre-design grey boxes on paper; audio/haptics/juice are Part 2.
- **Current phase:** 1.08 — **done to the limit this toolchain allows.** The ten rooms, the `ContentView` wiring, the per-room tests, and decisions D-033…D-039 are written. The deterministic logic is **verified against the real engine** by a standalone `swiftc` harness that compiles the actual model sources and replays all ten reference solutions, all three negatives, every hazard trace, the doors-closed unsolvability of all seven door-rooms, and D-020 path-legality (no echo/hazard path crosses a wall) — **130/130 checks pass**. The new test file additionally **type-checks clean** under CLT (real `RoomSolvabilityTests.swift` body compiled against the model sources + a minimal XCTest shim — exit 0). The iOS Simulator build, the on-device ⌘R run, and the **⌘U** XCTest run still need **Lazar's full Xcode** (this environment has only Command Line Tools — `swiftc` 6.4 is present; `xcodebuild`/`simctl` and the XCTest framework are not).
- **Carried forward (still owed, not done):** the iOS Simulator build, on-device ⌘R, the ⌘U XCTest run, the **D-025 `Levels/` bundling confirmation** on first ⌘R (now covering the ten room files), and the **1.02-carried toolchain pin** (exact Xcode 27 / iOS SDK build numbers) in `00_stack-and-config.md`.
- **Next:** Part 2 (feel: motion, audio, haptics, the real palette/accent/invert mode) — the ten rooms give Part 2 real content to tune against.

## Detail
- **`ECHO/App/ECHOApp.swift`** — `@main` SwiftUI `App`; opens a `WindowGroup` containing `ContentView`. Unchanged this phase. Real.
- **`ECHO/App/ContentView.swift`** — root view; **owns** the `GameState` (`@State`). **This phase the proof-room id list became the ten teaching rooms** (`["room-01", … , "room-10"]`); `Next` (`loadNextRoom()`) cycles them and the app loads `room-01` on launch via `ContentView.makeState(forRoomAt:)` (`LevelLoader.load` with a bare-board fallback). Debug bar unchanged: `Fold` / `Step back` (disabled at turn 0) / `Reset run` (keeps echoes) / `Clear` (wipes echoes, debug-only) / `Next`, the `turn N · echoes M/budget B` readout, and the **Solved ✓** stand-in. Still throwaway grey-box UI (D-017). Real.
- **`ECHO/Views/BoardView.swift`** — the board (grey lattice + grey-box element layers reading current-turn state). Unchanged this phase. Real.
- **`ECHO/Models/GameState.swift`** — `@MainActor @Observable` board state and the turn engine (`move`/`playerCollides`/`fold`/`restartRun`/`stepBack`/`clearEchoes` + pure derivations). **Unchanged this phase** — no new mechanics. The door-open check in `move(_:)` is read at the **pre-step** turn; the teaching rooms (notably 05 and 09) are authored to that rule (D-038/D-039). Real.
- **`ECHO/Models/Level.swift`** — `nonisolated` Decodable room schema (`Level` + `Switch` + `Door`) and `enum LevelLoader`. The locked v1 format (D-024). Unchanged this phase; the ten new rooms use exactly its key names. Real.
- **`ECHO/Models/Hazard.swift`** / **`Echo.swift`** / **`GridCoordinate.swift`** / **`Direction.swift`** — pure value types; `position(at:)` / `position(start:turn:)` drive the deterministic replay the rooms rely on. Unchanged this phase. Real.
- **`ECHO/Resources/Assets.xcassets`** — empty `AppIcon`/`AccentColor`. Unchanged.
- **`ECHO/Audio/`, `ECHO/Haptics/`** — reserved for Part 2; no files, not git-tracked while empty (no `.gitkeep`, D-012).
- **`Levels/room-01.json` … `room-10.json`** — the ten teaching rooms (D-033), in the locked v1 schema, bundled into the app target via the `Levels` synchronized root group (D-025). Room 05's geometry is the brief's (only the test's stall differs, D-038); room 09 is the **adjusted** 5×7 layout (D-039). The three `p1-06-*.json` proof rooms were deleted. Real.
- **`ECHOTests/ECHOTests.swift`** — the full 1.03–1.07 deterministic suite. Unchanged this phase. Real.
- **`ECHOTests/RoomSolvabilityTests.swift`** — **new this phase**: `@MainActor final class RoomSolvabilityTests: XCTestCase` (D-034). A `loadRoom(_:)` helper (app loader + source-tree fallback), an `assertSolves(…)` helper that replays a list of coordinate-path runs through `GameState` (asserting structural fields, that every move lands, each fold banks one echo, the final run wins, and folds ≤ budget), ten per-room tests, three negative tests (rooms 04/06/07), and a hazard-trace test. Real.
- **`docs/design-handovers/`** — reserved, empty (`.gitkeep`). No design handover for this phase (pre-design grey boxes).
- **`ECHO.xcodeproj`** — hand-authored, file-system synchronized groups (`ECHO`, `ECHOTests`, `Levels`). **Unchanged this phase** — because all three groups are file-system-synchronized, the ten new JSON files (under `Levels/`) and the new test file (under `ECHOTests/`) are picked up automatically with **no `.pbxproj` edit** required.

## Build & repo status
- **Project file:** `ECHO.xcodeproj` at repo root; SwiftUI App lifecycle; bundle id `com.dinovlazar.echo`; display name **Echo**; deployment target **iOS 17.0**; Swift language mode 6.0 (Swift 6.4 toolchain); app target sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (D-013); iPhone-only. App target bundles `ECHO` (sources + assets) and `Levels` (room JSON) via synchronized root groups. **No external packages (still zero).**
- **Signing:** "Automatically manage signing" on for both targets; **no `DEVELOPMENT_TEAM` set** (Lazar selects his Apple ID team at install time). Unchanged.
- **Verification performed here (Command Line Tools only; `swiftc` 6.4 present, no Xcode):**
  - **Engine replay (the ⌘U-equivalent for this phase's logic):** a standalone harness compiled from the **real** model sources (`GridCoordinate`/`Direction`/`Echo`/`Hazard`/`Level`/`GameState`) decoded all ten `Levels/room-*.json` through the real `Level` Decodable and replayed, against the real `GameState`: every reference solution (all win within budget), all three "naive run must fail" negatives (all restart), every hazard one-period trace + wrap, the doors-closed unsolvability of all seven door-rooms, and D-020 path-legality — **130/130 checks PASS**.
  - **Test type-check:** the real `ECHOTests/RoomSolvabilityTests.swift` body (with `import XCTest`/`@testable import ECHO` substituted by the model sources + a minimal XCTest shim) **type-checks clean — exit 0** — so it should compile under ⌘U.
  - **ContentView:** parses clean (`swiftc -parse` exit 0) after the room-list edit.
- **NOT verified here (needs Lazar's full Xcode):** the iOS Simulator build, the on-device ⌘R run, the **⌘U** XCTest run, a real screenshot, and — carried from 1.06 — that the **`Levels/` synchronized-group bundling** actually copies the JSON into the app bundle and the loader finds it at runtime (D-025; now ten files). **Still owed (carried from 1.02):** pin the exact **Xcode 27 / iOS SDK build numbers** in `00_stack-and-config.md`.
- **Remote:** `origin` = `https://github.com/DinovLazar/echo` (public). **Branch:** work for this phase is on a feature branch (see the completion report) → PR into `main`.

## Known issues
- **`.pbxproj` `Levels` bundling is still unverified here (D-025):** made blind in 1.06; the ten new rooms ride the same synchronized-group mechanism. Lazar should confirm on first ⌘R that rooms load (board shows walls/exit/hazard). If a room can't be read, the loader's bare-board fallback keeps the app running.
- **Two rooms deviate from the brief's letter (engine-driven, documented):** room 05's stall and room 09's grid (5×7, not 5×6) differ from the brief because the engine checks doors at the **pre-step** turn; both preserve budget and lesson (D-038/D-039). Flagged for the orchestrator to ratify.
- **⌘U not run in this env:** XCTest isn't available under CLT, so the per-room suite is verified here only by the equivalent standalone harness (130/130) plus a clean type-check of the real test file. Lazar should run ⌘U once in Xcode.
- **Reference tests assert solvability, not uniqueness (D-034):** each room test encodes one known-good solution; a room may have others. Redesigning a room means updating its test.
- **Throwaway debug bar is still live (D-017):** `Fold`/`Step back`/`Reset run`/`Clear`/`Next`/readout/`Solved ✓` must be removed in Parts 2–3; `Next` is a debug room-cycle, not Level Select (Part 3, D-037).
- **Authoring discipline (D-020/D-036):** echoes/hazards replay verbatim and ignore walls/doors; rooms are authored so nothing must traverse a wall or a door closed under it (harness-confirmed for all ten), and hazards teach **timing**, never blocking (an echo cannot stop a hazard).
- **Repo rule (D-012):** never put a `.gitkeep` (or any duplicate-named throwaway) inside `ECHO/` or `ECHOTests/`.
- `AppIcon` has no artwork yet; visuals are intentionally pre-design grey boxes — the real palette, accent, invert mode, and tuned motion are Part 2.
